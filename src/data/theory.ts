import type { TheorySection } from '@/types'

export const theorySections: TheorySection[] = [
  {
    id: 'what-is-cache',
    title: 'Что такое Web Cache',
    icon: 'Database',
    content: `**Web Cache (веб-кэш)** — это промежуточное хранилище, которое сохраняет копии часто запрашиваемых ресурсов, чтобы при следующем запросе не обращаться к серверу-источнику, а сразу вернуть сохранённую копию. Это как фотограф, который сделал снимок и потом показывает его всем желающим, вместо того чтобы каждый раз заново фотографировать один и тот же объект.

Представьте ситуацию: вы зашли на сайт интернет-магазина. Браузер загрузил логотип, стили, картинки. Если вы перейдёте на другую страницу того же сайта, логотип и стили загрузятся мгновенно — потому что браузер сохранил их у себя. Это и есть кэширование.

**Где может находиться кэш?** Это очень важный вопрос, потому что кэшей в цепочке между пользователем и сервером может быть НЕСКОЛЬКО:

- **Браузерный кэш** — хранит ресурсы прямо на вашем устройстве (в телефоне или компьютере). Когда вы повторно заходите на сайт, браузер может использовать локальные копии вместо загрузки из интернета.
- **CDN (Content Delivery Network)** — это сеть серверов по всему миру. Компания Cloudflare, Akamai, AWS CloudFront — всё это CDN. У Cloudflare, например, более 300 дата-центров по всему миру. Когда вы обращаетесь к сайту, запрос попадает на ближайший сервер CDN. Если CDN уже сохранил у себя копию страницы или файла — он отдаёт её мгновенно, не спрашивая настоящий сервер.
- **Обратный прокси (Reverse Proxy)** — это сервер, который стоит ПЕРЕД сервером-источником, в том же дата-центре. Nginx и Varnish — самые популярные обратные прокси. Они тоже могут кэшировать ответы сервера и отдавать их повторно без обращения к серверу-источнику.
- **Промежуточные прокси** — корпоративные или провайдерские прокси-серверы, которые кэшируют популярный контент для экономии трафика.

**Ключевой момент для понимания:** кэшей может быть несколько, и КАЖДЫЙ из них самостоятельно решает, что кэшировать. Если хотя бы один из них ошибётся и закэширует чужие данные — это уже уязвимость.`
  },
  {
    id: 'who-caches',
    title: 'Кто именно кэширует: Cloudflare, Nginx или Varnish?',
    icon: 'Server',
    content: `Это один из самых важных вопросов для понимания WCD. Ответ: **кэшировать может ЛЮБОЙ из них**, и часто кэширует НЕСКОЛЬКО одновременно. Давайте разберём каждый подробно.

**Cloudflare — кэширует ПО УМОЛЧАНИЮ**

Cloudflare — это CDN, и кэширование — его главная задача. Когда запрос проходит через Cloudflare, он автоматически проверяет: если URL выглядит как статический файл (заканчивается на .css, .js, .png, .jpg и т.д.), Cloudflare сохраняет ответ у себя. При следующем таком же запросе Cloudflare отдаст сохранённую копию, даже не спрашивая сервер.

Что именно Cloudflare считает "статическим" и кэширует:
- Файлы с расширениями: .css, .js, .png, .jpg, .gif, .svg, .ico, .woff, .woff2, .ttf, .mp4, .webm
- URL, которые заканчиваются этими расширениями
- При этом Cloudflare НЕ проверяет, что РЕАЛЬНО находится внутри ответа — он смотрит только на URL

**Nginx — кэширует ТОЛЬКО если это явно настроено**

Nginx может работать в двух режимах:
1. **Без кэширования** (по умолчанию) — Nginx просто передаёт запросы от Cloudflare к серверу-источнику и обратно. Как почтальон, который только доставляет письма, но не читает их.
2. **С кэшированием** (если настроен proxy_cache) — Nginx сохраняет ответы сервера и при повторных запросах отдаёт копии. Это как если бы почтальон завёл шкафчик и сохранял там копии писем.

Если Nginx настроен для кэширования, он кэширует файлы по правилам из конфига (обычно по расширениям, как и Cloudflare). Важная особенность: Nginx НОРМАЛИЗУЕТ URL перед проверкой, то есть удаляет спецсимволы вроде %0f из пути.

**Varnish — кэширует, и очень агрессивно**

Varnish — это специализированный кэширующий прокси. Он создаётся специально для кэширования и делает это очень быстро, храня кэш в оперативной памяти. Varnish настраивается на языке VCL (Varnish Configuration Language), и правила кэширования описываются программно.

Особенность Varnish: он НЕ нормализует URL. Если пришёл запрос /account/home%0f.css, Varnish видит .css в конце и кэширует, не пытаясь убрать спецсимволы.

**Типичная архитектура сайта:**

\`Пользователь → Cloudflare (кэширует) → Nginx (может кэшировать) → Сервер приложения\`

В этой цепочке Cloudflare — первый, кто видит запрос. Если Cloudflare решит кэшировать, он даже не передаст запрос на Nginx. Именно поэтому Cloudflare — самая частая цель WCD-атак: он кэширует автоматически и стоит первым на пути запроса.

**Но возможны и другие схемы:**
- Только Cloudflare без Nginx (многие сайты так работают)
- Только Nginx без Cloudflare (небольшие проекты)
- Только Varnish (высоконагруженные проекты)
- Cloudflare + Nginx + Varnish вместе (крупные платформы)

В ЛЮБОМ из этих вариантов атака WCD возможна — достаточно, чтобы хотя бы один уровень кэшировал неправильно.`
  },
  {
    id: 'why-caching',
    title: 'Зачем используется кэширование',
    icon: 'Zap',
    content: `Кэширование решает несколько критически важных задач. Если бы не было кэширования, интернет работал бы намного медленнее, а серверы постоянно бы падали от перегрузки.

**1. Ускорение загрузки страниц**

Представьте: вы живёте в Москве, а сервер сайта находится в Нью-Йорке. Каждый ваш запрос летит через океан — это сотни миллисекунд туда и обратно. А теперь представьте, что в Москве есть сервер CDN (например, Cloudflare), который сохранил копию логотипа, стилей и картинок сайта. Вы запрашиваете страницу — и получаете эти файлы из Москвы за 5 миллисекунд вместо 200. Разница в 40 раз!

**2. Снижение нагрузки на серверы**

Популярный сайт может получать миллионы запросов в минуту. Если бы каждый запрос обрабатывал сервер-источник (запускал код, обращался к базе данных, генерировал HTML), сервер бы не справился. Кэш берёт на себя 80-90% повторяющихся запросов, и сервер обрабатывает только уникальные.

**3. Экономия денег на трафике**

Передача данных между дата-центрами стоит денег. Если Cloudflare сохранил копию в Москве, ему не нужно каждый раз скачивать её из Нью-Йорка. Это экономит огромные суммы для крупных проектов.

**4. Защита от падения сервера**

Если сервер-источник временно упал, но кэш уже сохранил его страницы — пользователи всё равно смогут пользоваться сайтом. Кэш будет отдавать последние сохранённые версии, пока сервер не поднимется.

**5. Защита от пиковых нагрузок**

Во время распродаж (Чёрная пятница, Новый год) трафик может вырасти в 100 раз. Без кэша серверы бы просто упали. Кэш поглощает основную волну, отдавая сохранённые копии.

**Обратная сторона кэширования:**

Кэш не знает, ЧТО именно он сохраняет. Он видит URL, решает "это статический файл" — и сохраняет. Если URL выглядит как .css, но сервер вернул профиль пользователя с паролями и API-ключами — кэш сохранит и это. А потом отдаст любому, кто попросит. Именно это и есть атака Web Cache Deception.`
  },
  {
    id: 'what-is-wcd',
    title: 'Что такое Web Cache Deception',
    icon: 'ShieldAlert',
    content: `**Web Cache Deception (WCD)** — это атака, при которой злоумышленник обманывает кэш, заставляя его сохранить конфиденциальные данные одного пользователя и выдать их другому.

Суть атаки проста: злоумышленник создаёт такой URL, который кэш считает "статическим файлом" (и решает его сохранить), а сервер-источник считает "страницей с секретными данными" (и возвращает профиль жертвы).

**Пошаговый пример для новичков:**

Представьте сайт банка. У него есть страница профиля: \`https://bank.com/account/home\`

Эта страница показывает ваши данные: имя, баланс, номер карты. Она секретная — каждый видит только свои данные (банк проверяет вашу авторизацию по cookie).

А теперь злоумышленник создаёт ссылку: \`https://bank.com/account/home%0f.css\`

Что происходит, когда жертва переходит по этой ссылке:

**Шаг 1.** Запрос попадает на Cloudflare (CDN). Cloudflare видит, что URL заканчивается на .css. Cloudflare думает: "О, это стили! Надо сохранить в кэш". Но в кэше пока пусто — первый запрос.

**Шаг 2.** Cloudflare пересылает запрос дальше, на сервер банка.

**Шаг 3.** Сервер банка получает запрос. Он видит путь /account/home — символ %0f (невидимый управляющий символ) сервер обрезает или игнорирует. Сервер думает: "Жертва хочет посмотреть свой профиль". Проверяет cookie жертвы — да, она авторизована. Возвращает её данные: имя, баланс, номер карты.

**Шаг 4.** Ответ проходит обратно через Cloudflare. Cloudflare видит: "URL заканчивается на .css, я сохраню это в кэш". И сохраняет ответ с данными жертвы, как будто это CSS-файл.

**Шаг 5.** Злоумышленник заходит по тому же URL: \`https://bank.com/account/home%0f.css\`

**Шаг 6.** Cloudflare находит в кэше сохранённый ответ и отдаёт его злоумышленнику. Без проверки авторизации! Потому что для Cloudflare это просто "CSS-файл", а CSS-файлы — публичные, их можно отдавать всем.

Итог: злоумышленник получил данные жертвы, не взламывая сервер и не обходя авторизацию. Он просто обманул кэш.

**Кто именно кэширует неправильно?** Это может быть Cloudflare, Nginx, Varnish — любой кэш в цепочке. Чаще всего это Cloudflare, потому что он стоит первым и кэширует автоматически. Но если Cloudflare не кэширует (например, сайт не использует CDN), то Nginx или Varnish тоже могут стать жертвой этой атаки — если они настроены на кэширование статических файлов.

**Почему это опасно?**
- Не нужно взламывать сервер — достаточно обмануть кэш
- Атаку сложно обнаружить — она не оставляет следов на сервере
- Данные могут быть доступны в кэше часами (TTL кэша может быть большим)
- Один закэшированный ответ могут получить тысячи людей`
  },
  {
    id: 'delimiter-discrepancies',
    title: 'Что такое Delimiter Discrepancies',
    icon: 'Split',
    content: `**Delimiter Discrepancies (расхождения разделителей)** — это главная причина, по которой возможна атака Web Cache Deception. Простыми словами: кэш и сервер по-разному понимают, где в URL заканчивается "настоящий путь" и начинается "мусор".

**Что такое разделитель?**

Разделитель — это символ, который разделяет URL на части. Например:
- / разделяет сегменты пути: /account/home — два сегмента
- ? отделяет путь от параметров: /search?q=test
- . обычно указывает на расширение файла: style.css
- # отделяет фрагмент: /page#section2

**Проблема:** некоторые спецсимволы кэш и сервер интерпретируют ПО-РАЗНОМУ. Для кэша это просто часть пути, а для сервера — разделитель, после которого всё игнорируется.

**Самые опасные разделители:**

|%0f|Невидимый символ Shift In|Кэш: просто часть пути, после него .css — значит, статический файл|Сервер: обрезает всё после %0f, видит /account/home|
|%0a|Перевод строки (\\n)|Кэш: просто часть пути|Сервер: обрезает путь по переводу строки|
|%00|Null-байт (\\0)|Кэш: просто часть пути|Сервер: обрезает путь по null-байту|
|;|Точка с запятой|Кэш: просто часть пути|Сервер (Java Spring): обрезает, это matrix-параметр|
|%0d|Возврат каретки (\\r)|Кэш: просто часть пути|Сервер: обрезает путь|

**Конкретный пример:**

URL: \`/account/home%0f.css\`

Что видит Cloudflare: "Путь /account/home%0f.css заканчивается на .css. Это статический CSS-файл. Кэшируем!"

Что видит сервер (Node.js/Express): "Символ %0f — это управляющий символ, я его обрезаю. Путь = /account/home. Это профиль пользователя. Возвращаю данные."

Результат: Cloudflare кэширует профиль пользователя как CSS-файл. Злоумышленник может получить его, просто зайдя по тому же URL.

**Почему Cloudflare не обрезает %0f?**

Потому что Cloudflare — это CDN, его задача — быстро обслуживать запросы. Он не парсит URL глубоко, а смотрит только на окончание: .css? Статический. Кэшируем. Всё. Cloudflare НЕ нормализует URL и не удаляет спецсимволы.

**Почему Nginx ДА обрезает %0f?**

Nginx — это полноценный веб-сервер с богатой логикой обработки URL. Когда Nginx получает запрос, он нормализует путь: декодирует URL-кодированные символы, удаляет лишние слэши, обрезает спецсимволы. Поэтому Nginx видит /account/home%0f.css, нормализует его и может получить /home.css (удалив %0f).

Именно из-за этой разницы — Cloudflare НЕ нормализует, Nginx нормализует — и возникает расхождение. Если перед Nginx стоит Cloudflare, то Cloudflare кэширует ответ ДО того, как Nginx его обработает. Nginx даже не увидит запрос — Cloudflare отдаст закэшированную копию.

**Именно поэтому важно понимать, кто именно кэширует в вашей инфраструктуре.** Если только Nginx — он нормализует URL и, возможно, не попадётся. Если Cloudflare — он не нормализует и попадётся почти наверняка.`
  },
  {
    id: 'url-interpretation',
    title: 'Как компоненты по-разному интерпретируют URL',
    icon: 'GitBranch',
    content: `Каждый компонент веб-инфраструктуры обрабатывает URL по своим правилам. Давайте разберём каждый подробно — это критически важно для понимания атаки.

**Cloudflare (CDN) — самый простой и самый уязвимый**

Cloudflare получает запрос ПЕРВЫМ. Его логика проста:
1. Посмотреть на окончание URL
2. Если заканчивается на .css, .js, .png, .jpg и т.д. — это "статический файл", кэшируем
3. Если нет — это "динамическая страница", не кэшируем

Cloudflare НЕ делает ничего сложного с URL:
- НЕ декодирует URL-кодированные символы (типа %0f)
- НЕ нормализует путь
- НЕ проверяет Content-Type ответа
- НЕ проверяет, что РЕАЛЬНО внутри файла

Поэтому URL /account/home%0f.css для Cloudflare — это просто "какой-то CSS-файл".

**Nginx (обратный прокси) — более умный, но тоже уязвимый**

Nginx стоит обычно между Cloudflare и сервером-источником. Его поведение зависит от конфигурации:

Если Nginx НАСТРОЕН для кэширования (proxy_cache), он:
1. Получает URL
2. НОРМАЛИЗУЕТ его: декодирует %0f, удаляет спецсимволы
3. Проверяет по регулярному выражению: заканчивается ли на .css, .js и т.д.
4. Если да — кэширует

Ключевое отличие: Nginx нормализует URL ДО проверки. Поэтому /account/home%0f.css после нормализации может стать /home.css — и Nginx решит кэшировать.

НО: если перед Nginx стоит Cloudflare, и Cloudflare УЖЕ закэшировал ответ — Nginx этот запрос даже не увидит. Cloudflare отдаст свою закэшированную копию.

**Varnish — гибкий, но опасный**

Varnish настраивается программно на языке VCL. По умолчанию он:
1. НЕ нормализует URL
2. Проверяет окончание URL по регулярному выражению
3. Если URL выглядит как статический — кэширует

Varnish особенно опасен, потому что:
- Он кэширует в оперативной памяти — очень быстро
- Он может кэшировать ответы, у которых нет заголовка Cache-Control
- Он удаляет Cookie для статических файлов (делая кэш общим для всех)

**Сервер-источник (Backend) — конечный получатель**

Backend получает запрос и обрабатывает URL по правилам своего фреймворка:

**Node.js (Express):** Декодирует URL автоматически. /account/home%0f.css декодируется в /account/home\\x0f.css. Символ \\x0f — невидимый, Express обрезает путь по нему, получается /account/home. Маршрут совпадает — возвращаются данные.

**Python (Flask):** Декодирует URL. /account/home%00.css обрезается по null-байту: /account/home. Flask возвращает профиль. /account/home%0a.css — \\n обрезает путь: /account/home.

**Java (Spring):** Обрабатывает ; как matrix-параметр. /account/home;.css обрезается до /account/home. Spring возвращает профиль.

**Ruby (Rails):** Точка в URL означает формат. /account/home.css — Rails ищет CSS-шаблон. Но /account/home%0a.css — Rack может обрезать по \\n: /account/home. Rails возвращает профиль.

**Сводная таблица: какой URL обманет какой компонент:**

| URL | Cloudflare | Nginx | Varnish | Backend |
|-----|-----------|-------|---------|---------|
| /home%0f.css | Кэширует (.css) | Нормализует | Кэширует (.css) | Обрезает %0f → /home |
| /home;.css | Кэширует (.css) | Кэширует (.css) | Кэширует (.css) | Spring обрезает ; → /home |
| /home%00.css | Кэширует (.css) | Может отклонить | Кэширует (.css) | Обрезает null → /home |
| /home%0a.css | Кэширует (.css) | Нормализует | Кэширует (.css) | Обрезает \\n → /home |

Видно, что Cloudflare и Varnish уязвимы почти ко ВСЕМ разделителям, потому что они не нормализуют URL. Nginx более защищён благодаря нормализации, но тоже может быть уязвим при определённой конфигурации.`
  },
  {
    id: 'data-leakage',
    title: 'Как возникает утечка данных',
    icon: 'AlertTriangle',
    content: `Давайте проследим полный путь атаки от начала до конца, чтобы понять, как именно утекают данные. Будем разбирать на конкретном примере.

**Исходные данные:**
- Сайт банка: bank.com
- Страница профиля: /account/home (возвращает имя, баланс, номер карты)
- Перед сайтом стоит Cloudflare (CDN) — кэширует статические файлы
- Сервер: Node.js + Express

**Шаг 1. Разведка**

Злоумышленник заходит на bank.com и изучает заголовки ответов. Он видит заголовок CF-Ray — значит, сайт использует Cloudflare. Он пробует открыть /account/home — видит страницу профиля с личными данными. Он проверяет, возвращает ли сервер заголовок Cache-Control: no-store — и обнаруживает, что НЕТ. Это уже плохо.

**Шаг 2. Конструирование вредоносного URL**

Злоумышленник знает, что Cloudflare кэширует файлы с расширением .css. Он добавляет к URL профиля разделитель %0f и расширение .css:

\`https://bank.com/account/home%0f.css\`

Почему именно %0f? Потому что:
- Cloudflare видит .css в конце → "статический файл, кэшируем"
- Node.js видит %0f → обрезает → "/account/home" → возвращает профиль

**Шаг 3. Приманка для жертвы**

Злоумышленник отправляет жертве ссылку. Это может быть:
- Письмо: "Ваш счёт заблокирован, проверьте: https://bank.com/account/home%0f.css"
- Комментарий на форуме банка
- Реклама на сайте

Жертва кликает. Она авторизована на bank.com (у неё есть cookie сессии).

**Шаг 4. Запрос проходит через Cloudflare**

Запрос жертвы: GET /account/home%0f.css

Cloudflare проверяет кэш — пусто (первый раз такой URL).
Cloudflare видит .css в конце → "Надо будет закэшировать ответ".
Пересылает запрос на сервер банка.

**Шаг 5. Сервер обрабатывает запрос**

Сервер Node.js получает: GET /account/home%0f.css

Express декодирует URL: /account/home\\x0f.css
Express обрезает по управляющему символу \\x0f: /account/home
Маршрут /account/home совпадает!
Проверяет cookie жертвы — она авторизована.
Возвращает данные жертвы: { "name": "Анна", "balance": "500000", "card": "4111..." }

**Шаг 6. Ответ возвращается через Cloudflare**

Ответ сервера проходит обратно через Cloudflare.
Cloudflare вспоминает: "URL заканчивается на .css, я решил кэшировать".
Cloudflare сохраняет ответ (с данными Анны!) в кэш.
Устанавливает TTL (время жизни кэша) — допустим, 1 час.
Отдаёт ответ жертве (она видит свои данные — пока всё нормально для неё).

**Шаг 7. Злоумышленник забирает данные**

Злоумышленник заходит: GET https://bank.com/account/home%0f.css

Cloudflare проверяет кэш — ЕСТЬ! Отдаёт закэшированный ответ.
Без проверки cookie! Без авторизации!
Потому что для Cloudflare это "CSS-файл", а CSS-файлы публичные.

Злоумышленник видит: { "name": "Анна", "balance": "500000", "card": "4111..." }

**Атака завершена.** Данные украдены.

**Кто виноват?**

- Сервер (Node.js/Express) — не установил заголовок Cache-Control: no-store. Если бы он это сделал, Cloudflare НЕ кэшировал бы ответ.
- Cloudflare — не нормализует URL и кэширует "вслепую", глядя только на расширение.
- Архитектура — отсутствие валидации URL на сервере (сервер должен отклонять запросы со спецсимволами).

**Как защититься:**

1. Сервер ДОЛЖЕН отправлять \`Cache-Control: no-store\` для всех страниц с личными данными. Это самое простое и надёжное решение.
2. Сервер должен отклонять запросы со спецсимволами в URL (%0f, %0a, %00, ;).
3. Cloudflare можно настроить на нормализацию URL (но это не всегда возможно).
4. Добавить проверку Content-Type ответа: если сервер вернул application/json, а URL заканчивается на .css — не кэшировать.`
  },
  {
    id: 'cache-headers-overview',
    title: 'HTTP-заголовки кэширования: кто и как управляет кэшем',
    icon: 'Cloud',
    content: `**HTTP-заголовки** — это главный механизм, с помощью которого сервер говорит кэшу, что можно сохранять, а что нельзя. Без понимания заголовков невозможно понять, почему кэш сохраняет приватные данные при атаке WCD и как это предотвратить.

**Что такое HTTP-заголовок?**

Каждый HTTP-ответ состоит из двух частей: статус-строка (например, HTTP/2 200 OK) и заголовки. Заголовки — это пары "ключ: значение", которые передают дополнительную информацию. Например:

\`HTTP/2 200 OK
Content-Type: text/html
Cache-Control: no-store
Set-Cookie: session=abc123\`

Здесь три заголовка: Content-Type говорит, что это HTML, Cache-Control запрещает кэширование, а Set-Cookie устанавливает cookie сессии.

**Кто создаёт заголовки кэширования?**

Заголовки кэширования создаёт **сервер-источник** (backend). Когда Node.js, Python, Java или любой другой сервер формирует ответ, он добавляет HTTP-заголовки. Это как отправитель письма, который пишет на конверте инструкции: "Не вскрывать до 1 января" или "Хранить в холодильнике".

Где именно в коде сервера создаются заголовки:

**Node.js (Express):**
\`res.set('Cache-Control', 'no-store').json(userData);\`
Функция res.set() добавляет заголовок к ответу.

**Python (Flask):**
\`response = jsonify({...})
response.headers['Cache-Control'] = 'no-store'
return response\`
Объект response имеет словарь headers, куда добавляются заголовки.

**Java (Spring):**
\`return ResponseEntity.ok()
    .cacheControl(CacheControl.noStore())
    .body(data);\`
Spring имеет встроенный класс CacheControl для удобной работы с заголовками.

**Ruby (Rails):**
\`response.headers['Cache-Control'] = 'no-store'
render json: {...}\`
Rails предоставляет прямой доступ к заголовкам через response.headers.

**Ключевая проблема:** ни один из этих фреймворков НЕ добавляет Cache-Control: no-store автоматически. Разработчик должен добавить его сам. Если забыл — кэш может сохранить приватные данные.

**Путь заголовка от сервера до кэша:**

1. Сервер-источник создаёт ответ с заголовком Cache-Control: no-store
2. Ответ проходит через обратный прокси (Nginx) — он видит заголовок и решает: кэшировать или нет
3. Ответ проходит через CDN (Cloudflare) — он тоже видит заголовок и решает
4. Ответ доходит до браузера — он тоже видит заголовок и решает: сохранять локально или нет

Каждый кэш в этой цепочке читает заголовки и принимает решение самостоятельно. Если сервер не отправил заголовок — кэш решает сам, и чаще всего решение будет "кэшировать".`
  },
  {
    id: 'cache-control-header',
    title: 'Cache-Control: самый важный заголовок',
    icon: 'Code',
    content: `**Cache-Control** — это самый важный HTTP-заголовок для управления кэшированием. Он появился в HTTP/1.1 и заменил устаревший заголовок Pragma. Именно Cache-Control говорит всем кэшам в цепочке (браузеру, CDN, прокси), что делать с ответом.

**Директивы Cache-Control, которые запрещают кэширование:**

\`Cache-Control: no-store\` — самая строгая директива. Говорит: "НИКОГДА не сохраняй этот ответ, ни в каком кэше". Ни браузер, ни CDN, ни прокси не должны хранить этот ответ даже временно. Это то, что нужно для защиты от WCD.

\`Cache-Control: no-cache\` — название обманчивое! Это НЕ значит "не кэшировать". Это значит: "можно сохранить, но ПЕРЕД использованием обязательно спроси сервер, не изменился ли ответ". Кэш делает условный запрос (If-None-Match / If-Modified-Since), и если сервер говорит "не изменился" (304 Not Modified), кэш использует сохранённую копию. Если изменился — сервер отправляет новый ответ.

\`Cache-Control: must-revalidate\` — похожа на no-cache, но строже: "можно сохранить, но если кэш устарел (прошёл max-age), ОБЯЗАТЕЛЬНО спроси сервер. Не используй устаревший кэш даже если сервер недоступен". Без must-revalidate кэш может отдать устаревший ответ, если сервер временно недоступен (stale-while-revalidate).

\`Cache-Control: proxy-revalidate\` — то же самое, что must-revalidate, но только для общих кэшей (CDN, прокси). Браузерный кэш может использовать устаревший ответ.

**Директивы Cache-Control, которые разрешают кэширование:**

\`Cache-Control: max-age=3600\` — "можно кэшировать на 3600 секунд (1 час)". Это время жизни кэша (TTL). После истечения max-age кэш считается устаревшим.

\`Cache-Control: s-maxage=7200\` — то же, что max-age, но только для общих кэшей (CDN, прокси). Браузер использует max-age, а CDN — s-maxage. Это позволяет давать CDN большее время жизни кэша.

\`Cache-Control: public\` — "этот ответ может быть сохранён в любом кэше, даже если запрос был с авторизацией". Обычно ответы на авторизованные запросы не кэшируются CDN, но public разрешает.

\`Cache-Control: private\` — "этот ответ предназначен только для одного пользователя, может быть сохранён ТОЛЬКО в браузерном кэше, но НЕ в CDN и прокси". Это важно: private запрещает CDN кэшировать, но браузер может.

**Комбинирование директив:**

Директивы можно комбинировать через запятую:

\`Cache-Control: no-store, no-cache, must-revalidate, proxy-revalidate\`

Это "максимальная защита" — именно такая комбинация используется в нашем проекте и рекомендуется для всех API-эндпоинтов с приватными данными. Давайте разберём, что делает каждая директива в этой комбинации:

- no-store: не сохранять в кэше НИКОГДА
- no-cache: даже если случайно сохранил — всегда спрашивай сервер
- must-revalidate: если кэш устарел — обязательно спроси сервер
- proxy-revalidate: то же для CDN и прокси

Зачем нужны все четыре, если no-store уже запрещает всё? Потому что разные кэши по-разному интерпретируют заголовки. Некоторые CDN игнорируют no-store (как Apache с CacheStoreNoStore On). Другие могут ошибочно сохранить ответ. Комбинация даёт многоуровневую защиту.

**Как Cache-Control влияет на атаку WCD:**

Если сервер отправляет Cache-Control: no-store → Cloudflare НЕ кэширует ответ → атака WCD НЕ срабатывает.

Если сервер НЕ отправляет Cache-Control (или отправляет только public) → Cloudflare решает сам → если URL выглядит как статический файл → кэширует → атака WCD срабатывает.

Именно поэтому Cache-Control: no-store — это ПЕРВАЯ и ГЛАВНАЯ линия защиты от Web Cache Deception.`
  },
  {
    id: 'other-cache-headers',
    title: 'Остальные заголовки: Expires, Pragma, Vary, Age, X-Cache',
    icon: 'Layers',
    content: `Помимо Cache-Control, существует несколько других HTTP-заголовков, которые влияют на кэширование. Одни устарели, другие — полезны для отладки. Разберём каждый подробно.

**Expires — устаревший предшественник Cache-Control**

\`Expires: Thu, 01 Dec 2024 16:00:00 GMT\`

Заголовок Expires указывает точную дату и время, когда ответ становится устаревшим. До этого момента кэш может использовать сохранённую копию без запроса к серверу. После — должен запросить заново.

Проблемы Expires: время на сервере и на кэше может не совпадать (часовые пояса, неправильная настройка часов). Если на сервере время 12:00, а на CDN 11:00 — CDN будет кэшировать на час дольше, чем ожидалось.

Expires был стандартом в HTTP/1.0, но в HTTP/1.1 его заменил Cache-Control: max-age. Если указаны оба заголовка, Cache-Control имеет приоритет. Использовать Expires в новых проектах НЕ рекомендуется.

Связь с WCD: если сервер отправил Expires в будущем, а Cache-Control не указан — CDN может закэшировать ответ. Если Expires в прошлом — CDN не будет кэшировать.

**Pragma — самый старый заголовок кэширования**

\`Pragma: no-cache\`

Заголовок Pragma пришёл из HTTP/1.0 и означает "не кэшировать". Он устарел и заменён на Cache-Control: no-cache. Современные кэши (Cloudflare, Nginx) Pragma обычно игнорируют, но некоторые старые прокси всё ещё его используют.

Рекомендация: добавляйте Pragma: no-cache для совместимости со старыми системами, но не полагайтесь на него как на основную защиту.

**Vary — ключ к пониманию, один ли кэш на всех**

\`Vary: Accept-Encoding\`

Заголовок Vary сообщает кэшу, по каким заголовкам запроса нужно различать кэшированные ответы. Представьте, что два пользователя запросили один и тот же URL, но один отправил Accept-Encoding: gzip, а другой — Accept-Encoding: br (Brotli). Сервер вернул разный контент (gzip и Brotli). Если Vary: Accept-Encoding указан — кэш сохранит ДВЕ версии ответа. Если нет — может отдать gzip-версию пользователю, который ожидает Brotli.

Другие значения Vary:

\`Vary: Cookie\` — кэшировать разные версии для пользователей с cookie и без. Это ЧАСТИЧНАЯ защита от WCD: если кэш различает ответы по Cookie, он не отдаст закэшированные данные авторизованного пользователя неавторизованному. Но! Некоторые CDN (особенно Cloudflare) УДАЛЯЮТ cookie для статических файлов, и тогда Vary: Cookie не работает.

\`Vary: Authorization\` — различать ответы по заголовку Authorization. Аналогично Cookie, но для Bearer-токенов.

\`Vary: *\` — означает "каждый запрос уникален, не кэшировать вообще". Эквивалент Cache-Control: no-store, но менее надёжный.

Почему Vary не спасает от WCD: когда Cloudflare видит URL, заканчивающийся на .css, он может удалить Cookie из запроса (считая, что CSS-файлам не нужна авторизация). Без Cookie кэш становится общим, и Vary: Cookie не помогает.

**Age — сколько времени ответ провёл в кэше**

\`Age: 3600\`

Заголовок Age добавляется кэшем (не сервером!) и показывает, сколько секунд ответ находится в кэше. Если Age: 3600 — ответ закэширован час назад. Если Age отсутствует — скорее всего, ответ свежий (прямо с сервера).

Как использовать Age для обнаружения WCD: если вы запросили /account/home%0f.css и увидели Age: 123 — значит, ответ из кэша, и он уже 123 секунды доступен всем. Это признак успешной атаки.

**X-Cache — отладочный заголовок кэша**

\`X-Cache: HIT\` или \`X-Cache: MISS\`

Заголовок X-Cache добавляется кэширующим прокси (Cloudflare, Varnish, некоторые конфигурации Nginx) и показывает, был ли ответ взят из кэша:

- HIT — ответ взят из кэша (быстро, но может быть устаревшим)
- MISS — ответ запрошен у сервера-источника (медленно, но свежий)
- EXPIRED — кэш устарел, запрашивается новый ответ
- STALE — кэш устарел, но используется, пока сервер недоступен
- BYPASS — кэш обойдён (запрос пошёл напрямую к серверу)

Как использовать X-Cache при тестировании WCD: отправляете запрос с вредоносным URL первый раз → X-Cache: MISS (кэш сохранил). Отправляете второй раз → X-Cache: HIT (ответ из кэша). Атака успешна!

В Cloudflare вместо X-Cache используется \`CF-Cache-Status\` с теми же значениями: HIT, MISS, EXPIRED, BYPASS, STALE.

**ETag и If-None-Match — валидация кэша**

\`ETag: "abc123"\`

ETag (Entity Tag) — это уникальный идентификатор версии ресурса. Сервер отправляет ETag с ответом. При следующем запросе браузер отправляет If-None-Match: "abc123". Если содержимое не изменилось — сервер отвечает 304 Not Modified (без тела), и браузер использует кэш. Если изменилось — сервер отправляет новый ответ с новым ETag.

Связь с WCD: ETag не защищает от WCD напрямую. Кэш всё равно может сохранить приватные данные. Но ETag помогает кэшу понять, что содержимое изменилось, и запросить свежую версию.`
  },
  {
    id: 'cache-headers-in-practice',
    title: 'Заголовки в нашем проекте: как защита работает на практике',
    icon: 'Shield',
    content: `Теперь давайте посмотрим, как заголовки кэширования реализованы в реальном проекте — прямо в нашей образовательной платформе. Мы применяем теорию на практике.

**Глобальная защита в next.config.ts:**

Наш проект добавляет Cache-Control ко ВСЕМ ответам автоматически:

\`async headers() {
  return [{
    source: "/(.*)",
    headers: [{
      key: "Cache-Control",
      value: "no-store, no-cache, must-revalidate, proxy-revalidate",
    }],
  }]
}\`

Что это делает: каждый HTTP-ответ от нашего сервера содержит Cache-Control с четырьмя директивами. Это значит, что даже если разработчик забыл добавить заголовок в конкретном API-роуте — глобальная настройка подстрахует.

Разберём каждую директиву: no-store запрещает кэшу сохранять ответ. no-cache требует повторной проверки перед использованием. must-revalidate обязывает кэш проверять актуальность при истечении срока. proxy-revalidate делает то же самое для CDN и прокси.

**Защита каждого API-роута:**

Помимо глобальной настройки, каждый API-эндпоинт нашего проекта также явно добавляет Cache-Control:

\`const NO_STORE_HEADERS = { 'Cache-Control': 'no-store, no-cache, must-revalidate' };\`

Почему двойная защита? Потому что Next.js может не применять глобальные заголовки к API-роутам в некоторых конфигурациях. Явное указание в каждом роуте гарантирует, что защита работает всегда.

**Заголовки безопасности в middleware:**

Наш middleware добавляет дополнительные заголовки безопасности:

\`response.headers.set('X-Frame-Options', 'DENY')
response.headers.set('X-Content-Type-Options', 'nosniff')
response.headers.set('Referrer-Policy', 'strict-origin-when-cross-origin')
response.headers.set('X-XSS-Protection', '1; mode=block')\`

Эти заголовки не связаны с кэшированием напрямую, но защищают от других атак: X-Frame-Options запрещает встраивание сайта в iframe (защита от clickjacking), X-Content-Type-Options запрещает браузеру угадывать тип контента (защита от MIME-sniffing), Referrer-Policy ограничивает передачу URL в заголовке Referer.

**Что будет, если убрать Cache-Control:**

Давайте представим, что мы убрали Cache-Control: no-store из API-эндпоинта /api/chat. Что произойдёт?

1. Пользователь отправляет сообщение в чат
2. Ответ от сервера проходит через CDN (если сайт деплоен на Vercel)
3. CDN видит: URL = /api/chat, Cache-Control не указан
4. CDN решает: "нет запрета на кэширование, давайте закэшируем"
5. Следующий пользователь, который отправит запрос к /api/chat, получит закэшированный ответ первого пользователя

Это в точности атака Web Cache Deception — только без разделителей! Если CDN кэширует ответ без проверки, приватные данные утекают.

**Как проверить заголовки в Burp Suite:**

Когда вы будете тестировать лабы PortSwigger через Burp, обращайте внимание на заголовки ответов. Вот что искать:

1. Cache-Control — есть ли no-store? Если нет — цель уязвима к WCD
2. X-Cache или CF-Cache-Status — показывает, из кэша ли ответ
3. Age — сколько секунд ответ в кэше
4. Vary — по каким заголовкам различаются кэшированные версии
5. Set-Cookie — если ответ закэширован с cookie, другой пользователь получит чужую сессию

**Чеклист для проверки заголовков при деплое:**

1. Все API-эндпоинты возвращают Cache-Control: no-store
2. Статические файлы (.css, .js) возвращают Cache-Control: public, max-age=31536000 (длинный TTL для файлов с хэшами)
3. HTML-страницы возвращают Cache-Control: no-store (чтобы пользователь всегда видел свежую версию)
4. Нет заголовка Cache-Control: public для API с приватными данными
5. X-Frame-Options: DENY или SAMEORIGIN для защиты от clickjacking
6. X-Content-Type-Options: nosniff для защиты от MIME-sniffing`
  },
  {
    id: 'nginx-config',
    title: 'Конфигурация Nginx: как прокси принимает решения',
    icon: 'Settings',
    content: `Nginx — один из самых распространённых веб-серверов и обратных прокси в мире. Его конфигурация напрямую влияет на то, будет ли он уязвим к WCD. Разберёмся, как именно Nginx решает, что кэшировать.

**Базовая конфигурация кэширования в Nginx**

Чтобы Nginx начал кэшировать, в конфиге должны быть указаны три вещи: путь к кэшу, ключ кэша и условия кэширования. Без этих настроек Nginx работает как простой прокси — передаёт запросы дальше, не сохраняя ответы.

Пример типичной конфигурации:

\`proxy_cache_path /var/cache/nginx levels=1:2 keys_zone=static_cache:10m max_size=1g inactive=60m;\`

Эта директива создаёт зону кэша: файлы хранятся в /var/cache/nginx, зона называется static_cache, занимает до 1 ГБ, файлы без обращений удаляются через 60 минут.

Затем в блоке server или location указывается, что именно кэшировать:

\`location ~* \\.(css|js|png|jpg|gif|svg|ico|woff|woff2|ttf)$ {
    proxy_cache static_cache;
    proxy_cache_valid 200 1h;
    proxy_pass http://backend;
}\`

Этот блок говорит: "Если URL заканчивается на .css, .js, .png и т.д. — закэшируй ответ на 1 час". Именно такая конфигурация создаёт уязвимость WCD, потому что Nginx проверяет только окончание URL.

**Ключ кэша (proxy_cache_key)**

По умолчанию ключ кэша — это \`$scheme$proxy_host$request_uri\`. Это означает, что два разных URL всегда будут иметь разные записи в кэше. URL /account/home.css и /account/home%0f.css — это ДВА РАЗНЫХ ключа в кэше Nginx.

Это важно: даже если сервер обрезает %0f и возвращает один и тот же контент для обоих URL, Nginx сохранит их как разные записи. Это означает, что закэшированный ответ по URL с %0f будет доступен только по этому же URL.

**Нормализация URL в Nginx**

Nginx выполняет несколько этапов нормализации URL до сопоставления с location:
1. Декодирует URL-кодированные символы (%20 → пробел, %2F → /)
2. Удаляет двойные слэши (// → /)
3. Удаляет точку и два слэша для защиты от directory traversal (../)
4. Обрезает URL по управляющим символам (%00, %0a, %0f)

Благодаря нормализации, URL /account/home%0f.css после обработки может превратиться в /account/home.css. И если есть location для .css — Nginx закэширует. Если нормализация обрежет .css — не закэширует. Всё зависит от конкретной версии Nginx и конфигурации.

**Директива proxy_cache_bypass**

Можно настроить Nginx так, чтобы он НЕ кэшировал ответы с определёнными условиями:

\`proxy_cache_bypass $cookie_session;\`

Если у пользователя есть cookie session, Nginx обойдёт кэш и запросит ответ у сервера. Это частичная защита — но она не работает, если злоумышленник заходит БЕЗ cookie (ведь кэш уже сохранён авторизованным пользователем).

**Директива proxy_hide_header и proxy_ignore_headers**

\`proxy_ignore_headers Cache-Control;\`

Эта директива говорит Nginx игнорировать заголовок Cache-Control от сервера. Если сервер отправил "Cache-Control: no-store", но Nginx это игнорирует — он всё равно закэширует ответ. Это ДЕЛАЕТ сайт уязвимым. К сожалению, некоторые администраторы добавляют эту директиву, чтобы "ускорить" сайт.

**Как правильно настроить Nginx для защиты от WCD:**

1. Не игнорируйте Cache-Control от сервера — уберите proxy_ignore_headers Cache-Control
2. Добавьте проверку Content-Type в ключ кэша: \`proxy_cache_key $scheme$proxy_host$request_uri$content_type;\`
3. Добавьте условие: не кэшировать, если Content-Type не совпадает с расширением URL
4. Включите нормализацию URL (она включена по умолчанию в современных версиях Nginx)
5. Добавьте заголовок X-Cache-Status в ответы для мониторинга: \`add_header X-Cache-Status $upstream_cache_status;\`

**Пример защищённой конфигурации:**

\`location ~* \\.(css|js|png|jpg|gif|svg|ico)$ {
    proxy_cache static_cache;
    proxy_cache_valid 200 1h;
    proxy_cache_key $scheme$proxy_host$request_uri;
    proxy_pass http://backend;

    # Не кэшировать, если Content-Type не статический
    if ($content_type !~* "text/css|application/javascript|image/") {
        proxy_cache_bypass 1;
    }
}\`

Важно понимать: даже с правильной конфигурацией Nginx, если перед ним стоит Cloudflare, Cloudflare может закэшировать ответ ДО того, как запрос дойдёт до Nginx. Защита на уровне Nginx работает только если Nginx — единственный кэш в цепочке.`
  },
  {
    id: 'apache-config',
    title: 'Конфигурация Apache: mod_cache и обработка URL',
    icon: 'FileCode',
    content: `Apache HTTP Server — один из старейших и самых распространённых веб-серверов. Его модульная архитектура делает настройку кэширования гибкой, но и создаёт дополнительные векторы для WCD-атак.

**Модули кэширования Apache**

Apache имеет два основных модуля для кэширования:

**mod_cache** — основной модуль кэширования. Он работает как оболочка, которая решает, кэшировать ответ или нет, основываясь на HTTP-заголовках (Cache-Control, Expires) и собственных настройках. mod_cache делится на два провайдера:
- **mod_cache_disk** — сохраняет кэш на диск. Работает медленнее Varnish, но проще в настройке. Кэш переживает перезагрузку сервера.
- **mod_cache_socache** — сохраняет кэш в разделяемой памяти. Работает быстрее, но теряется при перезагрузке.

**mod_file_cache** — кэширует дескрипторы файлов (не содержимое). Ускоряет отдачу статических файлов, но не кэширует динамические ответы.

**Как mod_cache решает, что кэшировать**

По умолчанию mod_cache уважает заголовки Cache-Control и Expires от сервера. Если сервер отправил "Cache-Control: no-store", mod_cache НЕ будет кэшировать. Это лучше, чем поведение Cloudflare по умолчанию.

НО: если в конфиге Apache указана директива \`CacheStoreNoStore On\`, Apache будет игнорировать Cache-Control: no-store и кэшировать ВСЁ. Это критическая ошибка конфигурации.

Пример опасной конфигурации:

\`<IfModule mod_cache.c>
    CacheQuickHandler on
    CacheLock on
    CacheIgnoreHeaders Set-Cookie
    CacheStoreNoStore On
    CacheIgnoreCacheControl On
</IfModule>\`

Директива CacheIgnoreCacheControl On говорит Apache игнорировать ВСЕ заголовки Cache-Control от сервера. Это превращает Apache в кэш, который сохраняет вообще всё, включая персональные данные. CacheIgnoreHeaders Set-Cookie — ещё хуже: Apache удаляет заголовок Set-Cookie из закэшированного ответа, что делает кэш "общим" для всех пользователей.

**Обработка URL в Apache**

Apache обрабатывает URL через модуль mod_rewrite и систему Location/Directory. Правила RewriteRule применяются ДО проверки кэша, а директивы Location — ПОСЛЕ.

Вот ключевое отличие Apache от Nginx: Apache НЕ нормализует URL автоматически так же агрессивно. Символы вроде %0f, %00, %0a могут проходить через mod_rewrite без изменений, если нет явного правила для их обработки.

Пример: запрос /account/home%0f.css
- Apache получает URL как есть
- mod_rewrite может декодировать URL (если включён флаг [NE] — NoEscape, то НЕ декодирует)
- mod_cache проверяет окончание URL: .css → кэшируем
- Backend получает URL и обрабатывает по своим правилам

**Директива CacheEnable и CacheDisable**

\`CacheEnable disk /\`
Это включает дисковое кэширование для ВСЕХ URL. Опасно, если сервер отдаёт динамические страницы.

\`CacheDisable /account/\`
Это отключает кэширование для URL, начинающихся с /account/. Это частичная защита, но злоумышленник может использовать /account/home%0f.css — этот URL начинается с /account/, но mod_cache может его не распознать, потому что проверка идёт по результирующему пути после обработки mod_rewrite.

**Модуль mod_rewrite и его роль в WCD**

mod_rewrite — мощный инструмент для преобразования URL, но его неправильная настройка может создать уязвимость:

\`RewriteEngine On
RewriteRule ^(.*\\.css)$ /static/$1 [L]\`

Это правило перенаправляет ВСЕ запросы, заканчивающиеся на .css, в директорию /static/. Если злоумышленник отправит /account/home%0f.css, Apache сопоставит это с шаблоном и перенаправит в /static/account/home%0f.css. Если такого файла нет — Apache вернёт 404. Но если конфигурация кэширует 404 ответы, это тоже может быть проблемой.

Более опасный сценарий: если RewriteRule НЕ срабатывает (URL не попадает под шаблон), запрос передаётся на backend как есть. Backend обрезает %0f, возвращает профиль, и Apache кэширует его, потому что URL заканчивается на .css.

**Как правильно настроить Apache для защиты от WCD:**

1. Никогда не используйте CacheStoreNoStore On и CacheIgnoreCacheControl On
2. Всегда передавайте заголовок Cache-Control: no-store от сервера
3. Используйте CacheDisable для динамических страниц: \`CacheDisable /api/ /account/ /profile/\`
4. Добавьте правила mod_rewrite для отклонения запросов со спецсимволами:
   \`RewriteCond %{REQUEST_URI} %[0-9a-fA-F]{2} [NC]
   RewriteRule .* - [F]\`
5. Настройте CacheKeyModifier, чтобы включить Content-Type в ключ кэша
6. Включите заголовок Age в ответы для мониторинга: по нему можно понять, сколько времени ответ провёл в кэше

**Сравнение Apache и Nginx в контексте WCD:**

| Характеристика | Apache | Nginx |
|---|---|---|
| Нормализация URL | Слабая (зависит от mod_rewrite) | Агрессивная (встроена) |
| Уважение Cache-Control | Да (по умолчанию) | Да (по умолчанию) |
| Игнорирование Cache-Control | CacheIgnoreCacheControl On | proxy_ignore_headers Cache-Control |
| Кэширование по расширению | CacheEnable + Location | proxy_cache + location |
| Сложность настройки | Выше (модульная система) | Ниже (единый конфиг) |

Apache более уязвим к WCD, чем Nginx, из-за слабой нормализации URL. Но при правильной настройке оба сервера могут быть защищены. Главное правило: никогда не игнорируйте Cache-Control от сервера и всегда валидируйте URL.`
  },
  {
    id: 'cloudflare-config',
    title: 'Cloudflare: правила кэширования и настройки безопасности',
    icon: 'Cloud',
    content: `Cloudflare — самый популярный CDN в мире, обслуживающий более 20% всего веб-трафика. Его настройки кэширования оказывают огромное влияние на безопасность сайта от WCD-атак. Разберёмся, как Cloudflare решает, что кэшировать, и как его правильно настроить.

**Как Cloudflare решает, что кэшировать**

Cloudflare кэширует ответы на основе двух факторов: расширения файлов в URL и HTTP-заголовков ответа. По умолчанию Cloudflare кэширует файлы со следующими расширениями: .css, .js, .png, .jpg, .jpeg, .gif, .svg, .ico, .woff, .woff2, .ttf, .otf, .mp4, .webm, .webp, .pdf и другие.

Важно: Cloudflare НЕ проверяет Content-Type ответа. Если URL заканчивается на .css, но сервер вернул application/json с персональными данными — Cloudflare всё равно закэширует этот ответ. Это главная причина, почему Cloudflare так уязвим к WCD.

**Cache-Level Settings**

В настройках Cloudflare (Dashboard → Caching → Configuration) есть три уровня кэширования:
- **No Query String** — кэшировать, только если в URL нет строки запроса (?key=value). Самый агрессивный уровень.
- **Ignore Query String** — кэшировать все ресурсы, игнорируя строку запроса. Все варианты URL с разными параметрами будут считать одним и тем же ресурсом.
- **Standard** — кэшировать все ресурсы с уникальными строками запроса. Разные параметры = разные записи в кэше.

Для защиты от WCD лучше использовать Standard — это хотя бы разделяет кэш для разных query-параметров.

**Page Rules и Cache Rules**

Cloudflare предлагает два механизма для управления кэшированием:

**Page Rules** — старый механизм, ограниченный 3-мя правилами на бесплатном плане. Позволяет задать шаблон URL и действие: Cache Level (Bypass, No Query String, Standard, Cache Everything), Edge Cache TTL, Browser Cache TTL.

Пример защитного правила Page Rule:
\`URL: *bank.com/account/*
Cache Level: Bypass\`

Это правило говорит Cloudflare: "Не кэшировать НИЧЕГО по URL, начинающимся с /account/". Но есть проблема: это правило НЕ сработает для /account/home%0f.css, потому что Cloudflare видит .css в конце и кэширует ДО проверки Page Rules (в некоторых конфигурациях).

**Cache Rules** — новый механизм (заменяет Page Rules для кэширования). Более гибкий, поддерживает условия на основе заголовков, cookies, пути URL. Позволяет создавать сложную логику.

Пример защитного Cache Rule:
\`If the URI Path matches "/account/*"
Then: Bypass cache\`

Cache Rules надёжнее Page Rules, потому что они проверяются РАНЬШЕ в цепочке обработки запроса. Но бесплатный план позволяет создать только 10 правил.

**Edge Cache TTL и Browser Cache TTL**

Edge Cache TTL — как долго Cloudflare хранит кэш на своих серверах. По умолчанию для статических файлов — 2 часа. Если WCD-атака удалась, данные жертвы будут храниться в кэше 2 часа, и любой сможет их получить.

Browser Cache TTL — как долго браузер пользователя хранит кэш. Для WCD менее важно, но тоже имеет значение.

**Cloudflare и нормализация URL**

Критический вопрос: нормализует ли Cloudflare URL? Ответ: по умолчанию — НЕТ. Cloudflare передаёт URL как есть, не декодируя %0f, %00, %0a и другие спецсимволы. Это делает Cloudflare уязвимым к delimiter discrepancies.

В 2023 году Cloudflare добавил опцию URL Normalization в настройках (Dashboard → Rules → Normalization), но она ВЫКЛЮЧЕНА по умолчанию. Когда включена, Cloudflare нормализует URL до проверки кэша — и это закрывает большинство WCD-векторов.

**Настройки нормализации:**
- **None** — нет нормализации (по умолчанию). Уязвимо.
- **Basic** — декодирует URL-кодированные символы, удаляет двойные слэши. Частично защищает.
- **Full** — полная нормализация как в Nginx. Рекомендуется для защиты от WCD.

**Cloudflare Workers и WCD**

Cloudflare Workers — это серверные функции, выполняющиеся на edge-серверах Cloudflare. С помощью Workers можно реализовать кастомную логику кэширования:

\`addEventListener('fetch', event => {
  const url = new URL(event.request.url);
  // Если URL содержит спецсимволы — не кэшировать
  if (/%[0-9a-fA-F]{2}/.test(url.pathname)) {
    event.respondWith(fetch(event.request));
    return;
  }
  // Остальные запросы — обычная обработка
  event.respondWith(fetch(event.request));
});\`

Workers выполняются ДО проверки кэша, поэтому они могут перехватить вредоносный URL и запретить кэширование. Это самый надёжный способ защиты от WCD на Cloudflare, но он требует платного плана (Workers Paid).

**Рекомендации по настройке Cloudflare для защиты от WCD:**

1. Включите URL Normalization → Full в настройках (Dashboard → Rules → Normalization)
2. Создайте Cache Rule: Bypass cache для всех динамических путей (/account/*, /api/*, /profile/*)
3. Настройте Cache Rule: не кэшировать ответы с Content-Type, не совпадающим с расширением URL
4. Используйте Cloudflare Workers для кастомной валидации URL (если позволяет план)
5. Убедитесь, что сервер отправляет Cache-Control: no-store для всех чувствительных страниц
6. Установите минимальный Edge Cache TTL для динамических страниц (если они случайно закэшируются, TTL должен быть минимальным)
7. Мониторьте заголовки ответов: CF-Cache-Status должен быть MISS или DYNAMIC для динамических страниц, HIT — только для настоящих статических файлов`
  },
  {
    id: 'varnish-config',
    title: 'Varnish: VCL и логика кэширования',
    icon: 'Code',
    content: `Varnish — специализированный кэширующий обратный прокси, созданный для максимальной производительности. Он хранит кэш в оперативной памяти и обслуживает десятки тысяч запросов в секунду. Его конфигурационный язык VCL (Varnish Configuration Language) даёт полную власть над логикой кэширования — но с большой властью приходит и большая ответственность.

**Архитектура Varnish**

Varnish работает по принципу "запрос → проверка кэша → если есть, отдаём из кэша; если нет, запрашиваем у сервера и сохраняем". Кэш хранится в оперативной памяти, что делает его невероятно быстрым — типичный запрос обслуживается за 0.1-1 мс.

Varnish имеет несколько этапов обработки запроса:
1. **vcl_recv** — получение запроса от клиента. Здесь решаем: искать в кэше или идти к серверу.
2. **vcl_hash** — вычисление ключа кэша. По умолчанию — URL + Host.
3. **vcl_hit** — найдено в кэше. Отдаём.
4. **vcl_miss** — не найдено. Запрашиваем у сервера.
5. **vcl_backend_response** — получили ответ от сервера. Решаем: кэшировать или нет.
6. **vcl_deliver** — отдаём ответ клиенту.

**Как Varnish решает, что кэшировать**

По умолчанию Varnish НЕ кэширует запросы с cookie и запросы, требующие авторизации. Это хорошая защита — но она легко ломается неправильной конфигурацией.

Типичная уязвимая конфигурация:

\`sub vcl_recv {
    # Удаляем cookie для "статических" файлов
    if (req.url ~ "\\.(css|js|png|jpg|gif|svg|ico)$") {
        unset req.http.Cookie;
    }
}\`

Это правило говорит: "Если URL заканчивается на .css, .js и т.д. — удали cookie из запроса". Зачем? Чтобы Varnish кэшировал статические файлы, даже если у пользователя есть cookie. Проблема: если URL /account/home%0f.css попадает под это правило, Varnish удалит cookie, запросит у сервера данные авторизованного пользователя и закэширует ответ БЕЗ привязки к конкретному пользователю.

**VCL: vcl_recv и удаление cookie**

Это самая частая причина WCD в Varnish. Многие администраторы добавляют правило удаления cookie для статических файлов, не понимая последствий. Правильный подход — удалять cookie ТОЛЬКО если мы УВЕРЕНЫ, что файл действительно статический:

\`sub vcl_recv {
    # Удаляем cookie ТОЛЬКО для файлов из /static/ и /assets/
    if (req.url ~ "^/(static|assets)/.*\\.(css|js|png|jpg|gif|svg|ico)$") {
        unset req.http.Cookie;
    }
}\`

Это правило кэширует только файлы из конкретных директорий, а не все URL с расширением .css.

**VCL: vcl_backend_response и кэширование ответов**

В vcl_backend_response можно контролировать, какие ответы сервера кэшируются:

\`sub vcl_backend_response {
    # Не кэшировать, если сервер сказал no-store
    if (beresp.http.Cache-Control ~ "no-store") {
        return (pass);
    }

    # Не кэшировать, если Content-Type не совпадает с расширением URL
    if (req.url ~ "\\.css$" && beresp.http.Content-Type !~ "text/css") {
        return (pass);
    }

    # Установить TTL по умолчанию
    set beresp.ttl = 1h;
}\`

Проверка Content-Type — мощная защита: если URL заканчивается на .css, а Content-Type ответа application/json, значит, что-то не так, и кэшировать не нужно.

**VCL: vcl_hash и ключ кэша**

По умолчанию ключ кэша — это URL + Host. Можно добавить cookie в ключ, чтобы каждый пользователь имел свой кэш:

\`sub vcl_hash {
    hash_data(req.url);
    hash_data(req.http.Host);
    # Добавляем cookie авторизации в ключ кэша
    if (req.http.Cookie ~ "session=") {
        hash_data(regsub(req.http.Cookie, ".*session=([^;]+).*", "\\1"));
    }
}\`

Это частичная защита: каждый пользователь будет иметь свой кэш. Но злоумышленник без cookie всё равно получит закэшированный ответ авторизованного пользователя (если cookie был удалён в vcl_recv).

**Varnish и нормализация URL**

Varnish НЕ нормализует URL. URL /account/home%0f.css пройдёт через Varnish без изменений. Это делает Varnish уязвимым ко всем delimiter discrepancies.

Решение: добавить нормализацию в VCL:

\`sub vcl_recv {
    # Отклоняем запросы со спецсимволами
    if (req.url ~ "%[0-9a-fA-F]{2}" && req.url !~ "%20|%2F") {
        return (synth(400, "Invalid URL"));
    }
}\`

Это правило отклоняет все запросы с URL-кодированными спецсимволами, кроме %20 (пробел) и %2F (слэш), которые допустимы в URL.

**Рекомендации по настройке Varnish для защиты от WCD:**

1. Не удаляйте cookie для ВСЕХ "статических" файлов — только для конкретных директорий
2. Проверяйте Content-Type ответа в vcl_backend_response перед кэшированием
3. Уважайте Cache-Control: no-store от сервера
4. Добавьте нормализацию URL в vcl_recv
5. Добавьте cookie авторизации в ключ кэша (vcl_hash)
6. Установите beresp.grace (время, в течение которого устаревший кэш обслуживается) минимальным для динамических страниц
7. Логируйте все решения о кэшировании для мониторинга: \`std.log("CACHE: " + req.url + " decision=" + ...);\``
  },
  {
    id: 'frameworks-url-handling',
    title: 'Фреймворки: как каждый обрабатывает URL',
    icon: 'Layers',
    content: `Разные серверные фреймворки обрабатывают URL по-разному — и это ключевой фактор в атаке Web Cache Deception. Если кэш видит один URL, а фреймворк — другой, возникает расхождение, которым пользуется злоумышленник. Разберём популярные фреймворки подробно.

**Node.js — Express**

Express — самый популярный веб-фреймворк для Node.js. Он использует библиотеку path-to-regexp для сопоставления маршрутов, а встроенный модуль url для парсинга URL.

Поведение со спецсимволами:
- %0f (Shift In): Express декодирует URL-кодированные символы через decodeURIComponent. Символ \\x0f (невидимый управляющий символ) остаётся в пути. Когда Express ищет маршрут, он не находит маршрут с \\x0f в конце. Но если маршрут определён как /account/:id, Express может сопоставить /account/home\\x0f.css и вернуть профиль. Конкретное поведение зависит от определения маршрута.
- %00 (Null-байт): V8 (движок Node.js) обрезает строки по null-байту. Поэтому "home\\0.css" превращается в "home". Маршрут /account/home совпадает — возвращаются данные.
- %0a (Перевод строки): Аналогично, зависит от маршрутизации. Express обычно не обрезает по \\n автоматически, но некоторые middleware (например, express-urlrewrite) могут это делать.

Защита в Express:
\`app.use((req, res, next) => {
  if (/%[0-9a-fA-F]{2}/.test(req.path) && !/%20|%2F/.test(req.path)) {
    return res.status(400).send('Invalid URL');
  }
  next();
});\`

**Python — Flask**

Flask — микрофреймворк для Python, использующий библиотеку Werkzeug для обработки URL.

Поведение со спецсимволами:
- %00 (Null-байт): Werkzeug декодирует URL и обрезает путь по null-байту. /account/home%00.css → /account/home. Маршрут совпадает, Flask возвращает профиль.
- %0a (Перевод строки): Werkzeug декодирует %0a в \\n. Flask обрабатывает /account/home\\n.css — зависит от маршрута. Если маршрут точный (@app.route('/account/home')), \\n в конце не даст совпасть. Но если маршрут с параметром (@app.route('/account/<name>')), может совпасть.
- %0f: Аналогично Express — Werkzeug декодирует, но поведение зависит от определения маршрута.

Защита в Flask:
\`@app.before_request
def validate_url():
    import re
    if re.search(r'%[0-9a-fA-F]{2}', request.path) and not re.search(r'%20|%2F', request.path):
        return 'Invalid URL', 400\`

**Python — Django**

Django — полноценный веб-фреймворк с встроенной системой маршрутизации URL.

Поведение со спецсимволами:
- Django декодирует URL через urllib.parse.unquote. Символы %0f, %0a, %00 декодируются, но Django обычно не обрезает по ним путь автоматически. Вместо этого Django ищет точное совпадение маршрута — и если маршрут /account/home, то /account/home%0f.css НЕ совпадёт.
- НО: если использовать catch-all маршрут или регулярное выражение в url(), Django может сопоставить URL с вредоносными символами.
- Точка (.) в URL: Django обрабатывает особым образом — если в url() указан параметр с <str:name>, точка может быть включена в значение.

Защита в Django:
\`class URLValidationMiddleware:
    def __init__(self, get_response):
        self.get_response = get_response
    def __call__(self, request):
        import re
        if re.search(r'%[0-9a-fA-F]{2}', request.path) and not re.search(r'%20|%2F', request.path):
            return HttpResponse('Invalid URL', status=400)
        return self.get_response(request)\`

**Java — Spring Boot**

Spring Boot — самый популярный фреймворк для Java. Его обработка URL существенно отличается от других фреймворков из-за поддержки matrix-параметров.

Поведение со спецсимволами:
- ; (Точка с запятой): Spring обрабатывает всё после ; как matrix-параметр и обрезает его. /account/home;.css → /account/home. Маршрут совпадает, Spring возвращает профиль. Это УНИКАЛЬНАЯ уязвимость Spring — другие фреймворки не обрабатывают ; как разделитель.
- %00 (Null-байт): Tomcat (сервер, на котором обычно работает Spring) отклоняет null-байты в URL с ошибкой 400. Это защита.
- %0f: Spring декодирует URL, но поведение зависит от настроек URLDecode. По умолчанию Spring нормализует URL и может отклонить запросы с управляющими символами.
- %23 (#): Spring обрезает URL по #, как и ожидается.

Защита в Spring Boot:
\`@Configuration
public class WebConfig implements WebMvcConfigurer {
    @Override
    public void configurePathMatch(PathMatchConfigurer configurer) {
        configurer.setUseTrailingSlashMatch(true);
        configurer.setUseSuffixPatternMatch(false);
    }
}

@Component
public class URLValidationFilter extends OncePerRequestFilter {
    @Override
    protected void doFilterInternal(HttpServletRequest request,
            HttpServletResponse response, FilterChain chain)
            throws ServletException, IOException {
        String uri = request.getRequestURI();
        if (uri.contains(";") || uri.contains("%00") || uri.contains("%0f")) {
            response.sendError(400, "Invalid URL");
            return;
        }
        chain.doFilter(request, response);
    }
}\`

**Ruby — Ruby on Rails**

Rails — фреймворк для Ruby, использующий Rack как прослойку между веб-сервером и приложением.

Поведение со спецсимволами:
- . (Точка): Rails обрабатывает точку как указатель формата. /account/home.css означает "запросить профиль в формате CSS". Если CSS-шаблон существует — Rails вернёт его. Если нет — ошибка 406 Not Acceptable.
- %0a (Перевод строки): Rack может обрезать URL по \\n. /account/home%0a.css → /account/home. Маршрут совпадает.
- %00: Rack обрезает по null-байту. /account/home%00.css → /account/home.
- ; (Точка с запятой): Rails НЕ обрезает по ;. Это отличие от Spring.

Защита в Rails:
\`# config/application.rb
config.middleware.insert_before 0, Rack::RequestForgeryProtection
# Или кастомный middleware:
class URLValidationMiddleware
  def initialize(app)
    @app = app
  end
  def call(env)
    path = env['PATH_INFO']
    if path =~ /%[0-9a-fA-F]{2}/ && path !~ /%20|%2F/
      return [400, {'Content-Type' => 'text/plain'}, ['Invalid URL']]
    end
    @app.call(env)
  end
end\`

**PHP — Laravel**

Laravel — самый популярный PHP-фреймворк, использующий Symfony Routing Component.

Поведение со спецсимволами:
- %00: PHP обрезает строки по null-байту (унаследованное поведение от C). /account/home%00.css → /account/home. Маршрут совпадает.
- %0f: Laravel декодирует URL, но обычно не обрезает по управляющим символам. Поведение зависит от версии PHP и настроек.
- . (Точка): Laravel обрабатывает точку как часть имени маршрута, а не как разделитель. /account/home.css — маршрут /account/home не совпадёт.

Защита в Laravel:
\`// app/Http/Middleware/ValidateURL.php
class ValidateURL {
    public function handle($request, Closure $next) {
        if (preg_match('/%[0-9a-fA-F]{2}/', $request->path())
            && !preg_match('/%20|%2F/', $request->path())) {
            return response('Invalid URL', 400);
        }
        return $next($request);
    }
}\`

**Go — стандартная библиотека (net/http)**

Go — язык с встроенным HTTP-сервером. Его обработка URL через пакет net/url достаточно строгая.

Поведение со спецсимволами:
- %00: Go НЕ обрезает строки по null-байту автоматически. Но http.ServeMux строго сопоставляет маршруты — /account/home.css НЕ совпадёт с /account/home.
- %0f: Go декодирует URL, но ServeMux ищет точное совпадение. Вредоносные символы могут не дать маршруту совпасть.
- Спецсимволы: Go более строг к URL, чем другие фреймворки. Он отклоняет некорректные URL ещё на этапе парсинга.

Go — один из самых безопасных фреймворков в контексте WCD благодаря строгому парсингу URL.

**ASP.NET — C#**

ASP.NET Core — фреймворк от Microsoft для платформы .NET.

Поведение со спецсимволами:
- %00: ASP.NET отклоняет null-байты в URL с ошибкой 400. Защита встроена.
- ; (Точка с запятой): ASP.NET НЕ обрезает по ;. URL /account/home;.css будет искать маршрут /account/home;.css — скорее всего, 404.
- %0f: ASP.NET декодирует URL, но строгий роутинг не даст маршруту совпасть с вредоносным URL.

ASP.NET Core достаточно защищён от WCD благодаря строгому роутингу и отклонению некорректных символов.

**Сводная таблица уязвимостей фреймворков:**

| Фреймворк | %0f | %00 | %0a | ; | . | Защищённость |
|---|---|---|---|---|---|---|
| Express (Node.js) | Зависит | Обрезает | Зависит | Нет | Нет | Средняя |
| Flask (Python) | Зависит | Обрезает | Зависит | Нет | Нет | Средняя |
| Django (Python) | Нет | Нет | Нет | Нет | Зависит | Выше средней |
| Spring Boot (Java) | Зависит | Отклоняет | Зависит | Обрезает | Нет | Низкая (из-за ;)| |
| Rails (Ruby) | Нет | Обрезает | Обрезает | Нет | Формат | Средняя |
| Laravel (PHP) | Зависит | Обрезает | Зависит | Нет | Нет | Средняя |
| Go (net/http) | Нет | Нет | Нет | Нет | Нет | Высокая |
| ASP.NET Core | Нет | Отклоняет | Нет | Нет | Нет | Высокая |

Вывод: Spring Boot (Java) — самый уязвимый фреймворк из-за обработки ; как matrix-параметра. Go и ASP.NET Core — самые защищённые благодаря строгому роутингу. Остальные фреймворки — в середине, и их уязвимость зависит от конкретной конфигурации маршрутов.`
  },
  {
    id: 'protection-strategies',
    title: 'Защита от WCD: стратегии и лучшие практики',
    icon: 'Shield',
    content: `Теперь, когда мы понимаем, как работает атака Web Cache Deception и какие компоненты уязвимы, давайте соберём все методы защиты в единую стратегию. Защита должна быть многоуровневой — нельзя полагаться только на один метод.

**Уровень 1: Сервер-источник (Backend)**

Это самый важный уровень. Если сервер правильно настроен, атака не сработает, даже если кэш скомпрометирован.

**1a. Заголовок Cache-Control: no-store**

Каждый ответ, содержащий персональные данные, ДОЛЖЕН содержать заголовок:

\`Cache-Control: no-store, no-cache, must-revalidate, proxy-revalidate\`

Это говорит ВСЕМ кэшам в цепочке: "Не сохраняй этот ответ". Cloudflare, Nginx, Varnish — все уважают этот заголовок (если только не настроены его игнорировать).

Пример для Express:
\`app.get('/account/*', (req, res) => {
  res.set('Cache-Control', 'no-store, no-cache, must-revalidate, proxy-revalidate');
  // ... возвращаем данные
});\`

Пример для Flask:
\`@app.route('/account/<path:path>')
def account(path):
    response = make_response(render_profile())
    response.headers['Cache-Control'] = 'no-store, no-cache, must-revalidate, proxy-revalidate'
    return response\`

**1b. Валидация URL на сервере**

Сервер должен отклонять запросы со спецсимволами в URL. Это "глубинная защита" — даже если кэш не нормализует URL, сервер не вернёт данные по вредоносному пути.

\`// Middleware для Express
app.use((req, res, next) => {
  const originalUrl = req.originalUrl;
  const decoded = decodeURIComponent(originalUrl);

  // Проверяем управляющие символы
  if (/[\\x00-\\x1f\\x7f]/.test(decoded)) {
    return res.status(400).send('Invalid URL: control characters detected');
  }

  // Проверяем matrix-параметры (для Spring-подобной защиты)
  if (decoded.includes(';')) {
    return res.status(400).send('Invalid URL: matrix parameters not allowed');
  }

  next();
});\`

**1c. Проверка Content-Type**

Если URL заканчивается на .css, но сервер собирается вернуть application/json — что-то не так. Сервер может обнаружить это несоответствие и отклонить запрос:

\`// Express middleware
app.use((req, res, next) => {
  const ext = req.path.split('.').pop();
  const dangerousExtensions = ['css', 'js', 'png', 'jpg', 'gif', 'svg', 'ico', 'woff', 'woff2', 'ttf'];

  if (dangerousExtensions.includes(ext) && !req.path.startsWith('/static/') && !req.path.startsWith('/assets/')) {
    return res.status(404).send('Not found');
  }

  next();
});\`

**Уровень 2: CDN (Cloudflare)**

**2a. Включить URL Normalization → Full**

Dashboard → Rules → URL Normalization → Full. Это заставит Cloudflare нормализовать URL до проверки кэша, закрывая большинство delimiter discrepancies.

**2b. Создать Cache Rules**

Создайте правила, которые отключают кэширование для динамических путей:

\`If: URI Path starts with "/account/" or "/api/" or "/profile/"
Then: Bypass cache\`

**2c. Использовать Cloudflare Workers**

Workers выполняются ДО кэширования и могут перехватить вредоносные URL:

\`export default {
  async fetch(request) {
    const url = new URL(request.url);
    const dangerousPatterns = [
      /%0[0-9a-fA-F]/,  // Управляющие символы
      /%1[0-9a-fA-F]/,  // Управляющие символы
      /\\x00/,            // Null-байт
    ];

    for (const pattern of dangerousPatterns) {
      if (pattern.test(url.pathname)) {
        return new Response('Invalid URL', { status: 400 });
      }
    }

    return fetch(request);
  }
};\`

**Уровень 3: Обратный прокси (Nginx/Varnish)**

**3a. Nginx: не игнорировать Cache-Control**

Убедитесь, что в конфиге НЕТ директивы proxy_ignore_headers Cache-Control. Если она есть — удалите её.

**3b. Nginx: добавить Content-Type в ключ кэша**

\`proxy_cache_key $scheme$proxy_host$request_uri$upstream_http_content_type;\`

Если URL заканчивается на .css, но Content-Type = application/json, ключ кэша будет другим — и злоумышленник не сможет получить закэшированные данные по тому же URL.

**3c. Varnish: не удалять cookie для "статических" файлов**

Вместо этого кэшируйте только файлы из конкретных директорий:

\`sub vcl_recv {
    if (req.url ~ "^/(static|assets)/") {
        unset req.http.Cookie;
    }
}\`

**Уровень 4: Архитектура**

**4a. Разделяйте статический и динамический контент**

Лучший способ избежать WCD — сделать так, чтобы динамические страницы НИКОГДА не имели расширений статических файлов:

- /account/home → /account/home (без расширения)
- Статика: /static/style.css, /assets/logo.png

Если все статические файлы лежат в /static/ и /assets/, а динамические пути не содержат точек — атака WCD становится невозможной, потому что вредоносный URL не будет совпадать ни с одним маршрутом сервера.

**4b. Мониторинг**

Настройте мониторинг заголовков ответов:
- CF-Cache-Status: HIT для динамических страниц = тревога
- X-Cache: HIT для /account/* = тревога
- Age: > 0 для страниц с Cache-Control: no-store = тревога

**Чеклист защиты от WCD:**

1. Все страницы с персональными данными возвращают Cache-Control: no-store
2. Сервер отклоняет запросы со спецсимволами в URL (%0f, %00, %0a, ;)
3. Сервер отклоняет запросы, где расширение URL не совпадает с Content-Type ответа
4. Cloudflare: включена URL Normalization → Full
5. Cloudflare: Cache Rules отключают кэширование для динамических путей
6. Nginx: НЕ игнорирует Cache-Control от сервера
7. Nginx: Content-Type включён в ключ кэша
8. Varnish: не удаляет cookie для всех "статических" URL
9. Статический контент отделён от динамического (/static/, /assets/)
10. Настроен мониторинг кэша (CF-Cache-Status, X-Cache, Age)`
  },
  {
    id: 'cloudflare-workers',
    title: 'Cloudflare Workers и Edge-кэширование',
    icon: 'Cloud',
    content: `**Cloudflare Workers** — это серверные функции, которые выполняются прямо на краевых серверах Cloudflare (edge-серверах), то есть максимально близко к пользователю. Представьте, что вы посадили маленького сотрудника в каждый из 300+ дата-центров Cloudflare по всему миру — и каждый такой сотрудник может перехватывать запросы, изменять их и управлять кэшем. Это невероятно мощно, но и создаёт новые векторы для WCD-атак.

**Как работают Cloudflare Workers?**

Worker — это JavaScript-код, который выполняется в изолированной среде (V8 isolate, как в Google Chrome). Когда пользователь отправляет запрос на ваш сайт, запрос СНАЧАЛА попадает на ближайший сервер Cloudflare. Если у вас подключён Worker, он может:
- Перехватить запрос ДО того, как Cloudflare примет решение о кэшировании
- Изменить URL запроса, заголовки или тело
- Решить, брать ответ из кэша или запрашивать у сервера-источника
- Модифицировать ответ сервера перед отправкой пользователю
- Программно управлять кэшем через Cache API

Это значит, что Worker может как ПОМОЧЬ защититься от WCD (проверяя URL и Content-Type), так и СОЗДАТЬ уязвимость (неправильно кэшируя ответы).

**Workers и кэширование: как они взаимодействуют**

По умолчанию Cloudflare кэширует запросы по тем же правилам, что и без Worker: проверяет расширение URL, заголовки Cache-Control и т.д. Но Worker может ПОЛНОСТЬЮ переопределить логику кэширования с помощью Cache API:

\`caches.default\` — это глобальный кэш Cloudflare. Worker может искать в нём ответ, добавлять его или удалять. Пример: \`const cached = await caches.default.match(request)\`

\`caches.open(name)\` — создаёт именованную область кэша (аналог Cache API в браузере). Можно создавать отдельные кэши для разных типов контента.

**Пример уязвимого Worker:**

\`addEventListener('fetch', event => {
  const url = new URL(event.request.url)
  // Кэшировать ВСЁ, что заканчивается на .css — ОПАСНО!
  if (url.pathname.endsWith('.css')) {
    event.respondWith(
      caches.default.match(event.request).then(cached => {
        if (cached) return cached
        return fetch(event.request).then(response => {
          // Сохраняем в кэш БЕЗ проверки Content-Type!
          caches.default.put(event.request, response.clone())
          return response
        })
      })
    )
  }
})\`

Этот Worker кэширует любой ответ, если URL заканчивается на .css. Он НЕ проверяет, что реально внутри ответа. Если атакующий создаст URL /account/home%0f.css — Worker закэширует профиль пользователя как CSS-файл.

**Пример безопасного Worker:**

\`addEventListener('fetch', event => {
  const url = new URL(event.request.url)
  if (url.pathname.endsWith('.css')) {
    event.respondWith(
      caches.default.match(event.request).then(cached => {
        if (cached) return cached
        return fetch(event.request).then(response => {
          // Проверяем Content-Type ПЕРЕД кэшированием!
          const contentType = response.headers.get('Content-Type') || ''
          if (contentType.includes('text/css')) {
            caches.default.put(event.request, response.clone())
          }
          return response
        })
      })
    )
  }
})\`

Этот Worker проверяет, что Content-Type действительно text/css. Если сервер вернул application/json — Worker НЕ будет кэшировать ответ, даже если URL заканчивается на .css. Это простой и эффективный способ защиты.

**Workers и нормализация URL**

Критически важный момент: Cloudflare Workers НЕ нормализуют URL автоматически. Когда Worker получает объект Request, URL передаётся КАК ЕСТЬ — со всеми спецсимволами вроде %0f, %0a, %00. Объект URL в JavaScript декодирует %-кодировку, но не обрезает управляющие символы. Поэтому Worker должен САМ реализовать нормализацию:

\`addEventListener('fetch', event => {
  const url = new URL(event.request.url)
  // Нормализуем: удаляем спецсимволы из пути
  const cleanPath = url.pathname.replace(/[\\x00-\\x1f\\x7f]/g, '')
  if (cleanPath !== url.pathname) {
    // В URL были спецсимволы — отклоняем или перенаправляем
    return new Response('Bad Request', { status: 400 })
  }
})\`

**Почему Workers — новая поверхность атаки?**

- Worker может случайно закэшировать динамический контент (если программист забыл проверить Content-Type)
- Worker может создать "свой" кэш, который работает поверх стандартного кэша Cloudflare, и правила для них будут разными
- Worker работает на краевых серверах — закэшированный ответ распространится на конкретный дата-центр, но не на все 300+ одновременно (это частичная защита)
- Ошибки в логике Worker трудно отловить — они не видны в стандартных логах Cloudflare

**Рекомендации по безопасности Workers:**

1. Всегда проверяйте Content-Type перед кэшированием в Worker
2. Нормализуйте URL ДО проверки расширения
3. Не кэшируйте ответы с заголовком Cache-Control: no-store
4. Добавляйте заголовок X-Worker-Cache: HIT/MISS для мониторинга
5. Используйте отдельные кэши (caches.open) для разных типов контента`
  },
  {
    id: 'flyio-cloud-platforms',
    title: 'Fly.io и облачные платформы: кэширование без серверов',
    icon: 'Cloud',
    content: `**Fly.io** — это облачная платформа, которая позволяет запускать приложения на серверах по всему миру, максимально близко к пользователям. Fly.io отличается от традиционного хостинга тем, что ваше приложение разворачивается на "краевых" серверах (edge-серверах) — в дата-центрах, расположенных в десятках городов. Это похоже на Cloudflare Workers, но вместо маленьких JavaScript-функций вы запускаете полноценное приложение (Docker-контейнер).

**Fly.io и HTTP-кэширование**

Fly.io имеет встроенный механизм HTTP-кэширования через заголовок \`fly-cache-url\`. Когда вы настраиваете этот заголовок в своём приложении, Fly.io создаёт кэширующий прокси между пользователем и вашим приложением. Этот прокси работает аналогично Varnish: проверяет заголовки Cache-Control и Extensions URL для принятия решений о кэшировании.

Принцип работы:
1. Пользователь отправляет запрос на ваше приложение на Fly.io
2. Запрос проходит через обратный прокси Fly.io (аналог Nginx/Varnish)
3. Прокси проверяет: есть ли закэшированный ответ? Если да — отдаёт из кэша
4. Если нет — пересылает запрос в ваш контейнер, получает ответ и, если условия выполняются, сохраняет в кэш

**Как Fly.io обрабатывает URL**

Fly.io использует внутренний обратный прокси, который маршрутизирует запросы к вашему приложению. Этот прокси НЕ нормализует URL агрессивно. Спецсимволы вроде %0f, %00, %0a проходят через прокси и попадают в ваше приложение как есть. Если ваше приложение обрезает спецсимволы (например, Django обрезает по null-байту), а прокси Fly.io проверяет расширение URL — возникает расхождение, то есть классический вектор WCD.

**Fly.io Varnish Integration**

Fly.io позволяет подключить Varnish как sidecar-контейнер. Это означает, что вы можете запустить Varnish рядом с вашим приложением и настроить VCL-правила для кэширования. При такой конфигурации архитектура выглядит так:

\`Пользователь → Fly.io Proxy → Varnish (sidecar) → Ваше приложение\`

Varnish на Fly.io уязвим к WCD точно так же, как и обычный Varnish: он не нормализует URL, проверяет расширения регулярными выражениями и удаляет Cookie для "статических" файлов.

**Сравнение облачных платформ: кэширование и WCD**

| Платформа | Кэширование по умолчанию | Нормализация URL | WCD-вектор |
|-----------|--------------------------|------------------|------------|
| Fly.io | Через fly-cache-url или Varnish | Нет | Расширение URL + нет нормализации |
| Vercel | Edge Network кэширует статику | Частичная | Serverless Functions могут кэшировать |
| Netlify | CDN кэширует статику автоматически | Нет | .css/.js в URL = кэш |
| Railway | Нет встроенного кэша | Нет | Только если добавить Nginx/Varnish |
| Render | Нет встроенного кэша | Нет | Только если добавить Nginx/Varnish |
| AWS (CloudFront) | Кэширует по правилам | Можно настроить | Зависит от конфигурации |

**Vercel** — одна из самых популярных serverless-платформ. Vercel автоматически кэширует статические файлы (из папки /public) на своей Edge Network. Serverless Functions (API-маршруты) по умолчанию НЕ кэшируются, но если функция возвращает заголовки Cache-Control с положительным max-age — Vercel может закэшировать ответ. Проблема: Vercel НЕ нормализует URL, поэтому URL /api/user%0f.css может обмануть кэш Vercel, если API-маршрут обрабатывает %0f.

**Netlify** — аналогично Vercel: автоматически кэширует статику, не кэширует serverless-функции по умолчанию. Но Netlify Edge Functions (на базе Deno) МОГУТ кэшировать ответы программно. Если Edge Function использует Cache API и проверяет только расширение URL — это уязвимость WCD.

**Railway и Render** — платформы без встроенного кэширования. Если вы не настроили Nginx, Varnish или Cloudflare перед ними — кэшировать некому, и WCD-атака невозможна. Но многие проекты подключают Cloudflare перед Railway/Render, и тогда стандартные векторы Cloudflare работают.

**Специфические WCD-векторы на каждой платформе:**

- **Fly.io:** fly-cache-url + URL с %0f.css → прокси кэширует, приложение обрезает → утечка
- **Vercel:** API Route + Cache-Control: public + URL с %0f.css → Edge Network кэширует
- **Netlify:** Edge Function с Cache API + проверка по расширению → кэширует динамический контент
- **Railway/Render:** Без внешнего CDN — уязвимости нет. С Cloudflare — стандартные векторы

**Главный вывод:** облачные платформы НЕ защищают от WCD автоматически. Вы обязаны сами настраивать Cache-Control: no-store для динамических страниц и проверять URL на спецсимволы. Платформа лишь предоставляет инфраструктуру — а безопасность кэширования зависит от вашей конфигурации.`
  },
  {
    id: 'config-design-security',
    title: 'Проектирование конфигурационных файлов: принципы безопасности',
    icon: 'Settings',
    content: `Конфигурационный файл — это рецепт для вашего сервера. Если рецепт неправильный, блюдо будет испорчено. Если конфигурация уязвима — весь сайт уязвим. Давайте разберём, как проектировать безопасные конфигурации кэширования.

**Почему дизайн конфига важен для безопасности?**

Большинство WCD-уязвимостей возникают НЕ из-за багов в коде сервера, а из-за неправильных конфигурационных файлов. Nginx, Varnish, Apache — все они имеют мощные настройки кэширования, и одна неправильная директива может превратить безопасный сервер в уязвимый. Представьте, что вы строите дом: стены крепкие, двери надёжные, но вы забыли закрыть окно на втором этаже. Конфиг — это именно такое "окно".

**Принцип наименьших привилегий для кэша**

Этот принцип означает: кэш должен кэшировать ТОЛЬКО то, что ему явно разрешено. Не больше. По умолчанию — ничего не кэшировать, кэшировать только явно разрешённые пути.

Плохо: кэшировать всё, что выглядит как статический файл (по расширению):
\`location ~* \\.(css|js|png|jpg)$ { proxy_cache STATIC; }\`

Хорошо: кэшировать ТОЛЬКО файлы из определённых директорий:
\`location /static/ { proxy_cache STATIC; }
location /assets/ { proxy_cache STATIC; }\`

Разница огромная: первый вариант кэширует ЛЮБОЙ URL с .css в конце (включая /account/home%0f.css), а второй — только файлы из /static/ и /assets/, где не может быть динамических страниц.

**Подход Default-Deny: не кэшировать ничего по умолчанию**

Default-Deny (запрет по умолчанию) — это фундаментальный принцип безопасности. Применительно к кэшу он означает:
1. По умолчанию НИЧЕГО не кэшировать
2. Явно разрешить кэширование только для известных статических путей
3. Все остальные запросы — проксировать без кэша

Пример безопасного подхода в Nginx:
\`# По умолчанию — НЕ кэшировать
location / {
    proxy_pass http://backend;
    proxy_cache_bypass 1;
}

# Только конкретные статические пути — кэшировать
location /static/ {
    proxy_cache STATIC;
    proxy_cache_valid 200 1h;
}

location /assets/ {
    proxy_cache STATIC;
    proxy_cache_valid 200 1h;
}\`

**Валидация Content-Type в ключе кэша**

Даже если злоумышленник смог обмануть кэш на уровне URL, валидация Content-Type — это вторая линия обороны. Идея: если URL заканчивается на .css, а сервер вернул Content-Type: application/json — что-то не так, кэшировать нельзя.

В Nginx: \`proxy_cache_key "$scheme$host$uri$content_type";\`
В Varnish: \`hash_data(req.url + beresp.http.Content-Type);\`

**Нормализация URL перед cache lookup**

До того как принимать решение о кэшировании, URL должен быть нормализован:
1. Декодировать %-кодировку: %0f → управляющий символ
2. Удалить управляющие символы (\\x00-\\x1f)
3. Удалить повторяющиеся слэши (// → /)
4. Удалить точку-два-слэша (../)
5. Привести к нижнему регистру (опционально)

После нормализации URL /account/home%0f.css превращается в /account/home.css или /account/home (зависит от правил) — и решение о кэшировании принимается уже по нормализованному пути.

**Почему кэширование по расширению принципиально ошибочно**

Кэширование по расширению (.css, .js, .png) — это корень зла WCD. Почему оно опасно:
- Расширение в URL НЕ гарантирует, что файл действительно статический
- URL можно подделать: добавить .css к любому пути
- Разные компоненты по-разному интерпретируют спецсимволы в URL
- Проверка расширения НЕ учитывает Content-Type ответа

Правильный подход — **кэширование по префиксу пути** (path-prefix-based caching):
- Кэшировать ТОЛЬКО URL, начинающиеся с /static/, /assets/, /public/
- В этих директориях НЕТ динамических маршрутов — только настоящие файлы
- Любой URL вне этих директорий — динамический, не кэшировать

**Как структурировать конфиги для безопасности**

**nginx.conf:**
\`# Шаг 1: Определить зону кэша
proxy_cache_path /var/cache/nginx keys_zone=STATIC:10m;

# Шаг 2: Default-deny — не кэшировать по умолчанию
location / {
    proxy_pass http://backend;
    proxy_cache_bypass 1;
    add_header X-Cache-Status BYPASS;
}

# Шаг 3: Кэшировать ТОЛЬКО статические пути
location /static/ {
    proxy_cache STATIC;
    proxy_cache_key "$scheme$host$uri$content_type";
    proxy_cache_valid 200 1h;
    add_header X-Cache-Status $upstream_cache_status;
}

# Шаг 4: Отклонять запросы со спецсимволами
if ($request_uri ~* "%[0-1][0-9a-fA-F]") {
    return 400;
}\`

**default.vcl (Varnish):**
\`sub vcl_recv {
    # Нормализуем URL
    set req.url = regsuball(req.url, "[\\x00-\\x1f]", "");
    # Кэшируем ТОЛЬКО /static/ и /assets/
    if (req.url ~ "^/static/" || req.url ~ "^/assets/") {
        unset req.http.Cookie;
        return (hash);
    }
    return (pass);  # Всё остальное — не кэшировать
}\`

**Чеклист проверки конфигурационного файла:**

1. Кэширование отключено по умолчанию (default-deny)?
2. Кэшируются только известные статические пути (/static/, /assets/)?
3. Кэширование НЕ основано на расширении файла (.css, .js)?
4. Content-Type включён в ключ кэша?
5. URL нормализуется перед cache lookup?
6. Запросы со спецсимволами отклоняются?
7. Cache-Control: no-store от сервера НЕ игнорируется?
8. Cookie НЕ удаляется для всех "статических" URL?
9. Добавлены заголовки для мониторинга (X-Cache-Status, Age)?
10. Конфиг прошёл ревью безопасности?

**Таблица: безопасные vs опасные паттерны конфигурации:**

| Паттерн | Опасно | Безопасно |
|---------|--------|-----------|
| Что кэшировать | Всё с расширением .css/.js | Только /static/ и /assets/ |
| Ключ кэша | Только URL | URL + Content-Type |
| Нормализация | Нет | Да, перед cache lookup |
| Cache-Control | Игнорировать | Уважать |
| Cookie | Удалять для "статики" | Не удалять для неизвестных путей |
| Спецсимволы | Пропускать | Отклонять (400 Bad Request) |
| Логика | Extension-based | Path-prefix-based |`
  },
  {
    id: 'frameworks-url-handling',
    title: 'Как разные фреймворки обрабатывают запросы: Django, Laravel, Go, .NET',
    icon: 'Code',
    content: `Каждый веб-фреймворк по-своему обрабатывает URL — и это напрямую влияет на уязвимость к WCD. Давайте разберём четыре популярных фреймворка и выясним, какие из них более безопасны, а какие — менее.

**Django (Python): URL-диспетчер и %-кодировка**

Django — это "батарейка включена" фреймворк для Python. Он включает ORM, админку, систему аутентификации и, конечно, мощный URL-диспетчер (URL dispatcher).

Как Django обрабатывает URL:
- \`path('account/home/', views.account_home)\` — точное совпадение. Django ищет строку, которая ТОЧНО совпадает с шаблоном
- \`re_path(r'^account/(.*)$', views.account_view)\` — регулярное выражение. Более гибко, но и более опасно

**Django и %-кодировка:** Django декодирует URL-кодированные символы перед сопоставлением с маршрутами. Это значит, что /account/home%0f.css декодируется в /account/home\\x0f.css. Символ \\x0f — управляющий, и маршрут path('account/home/') НЕ совпадёт с /account/home\\x0f.css/. Поэтому точное совпадение (path()) ЗАЩИЩАЕТ от WCD с разделителями.

Но если используется \`re_path\` с catch-all:
\`re_path(r'^account/(.*)$', views.account_view)\`
Этот маршрут совпадёт с ЛЮБЫМ URL, начинающимся с /account/, включая /account/home%0f.css. И если view возвращает данные пользователя без Cache-Control: no-store — это уязвимость WCD.

**Настройка APPEND_SLASH:** Django имеет настройку APPEND_SLASH = True (по умолчанию). Это значит, что если запросить /account/home (без слэша), Django автоматически перенаправит на /account/home/ (со слэшем). Перенаправление — это 301/302 ответ, который кэш МОЖЕТ сохранить. Если кэш сохранит редирект для /account/home%0f.css, это менее опасно (редирект не содержит данных), но всё равно может создать проблемы.

**Django middleware для валидации URL:**
\`class RejectSpecialCharsMiddleware:
    def __init__(self, get_response):
        self.get_response = get_response

    def __call__(self, request):
        path = request.get_full_path()
        # Отклоняем URL с управляющими символами
        if re.search(r'[%][0-1][0-9a-fA-F]', path):
            return HttpResponseBadRequest('Invalid URL')
        return self.get_response(request)\`

**Laravel (PHP): маршрутизация и URL-нормализация**

Laravel — самый популярный PHP-фреймворк. Он использует Symfony Routing Component для обработки URL, что даёт мощную и гибкую систему маршрутизации.

Как Laravel обрабатывает URL:
- \`Route::get('/account/home', [AccountController::class, 'home'])\` — точное совпадение
- \`Route::get('/account/{name}', [AccountController::class, 'show'])\` — параметр в URL
- Laravel ищет ПЕРВОЕ совпадение в файле routes/web.php

**Laravel и Illuminate\\Routing:** Маршрутизатор Laravel (Illuminate\\Routing) обрабатывает URL через несколько этапов:
1. Получает URL из запроса
2. Декодирует %-кодировку
3. Ищет совпадение с зарегистрированными маршрутами
4. Если совпадение найдено — вызывает контроллер

**Как Laravel обрабатывает .css/.js расширения в маршрутах:**
Laravel ищет ТОЧНОЕ совпадение маршрута. Маршрут /account/home НЕ совпадёт с /account/home.css — Laravel вернёт 404. Это хорошая новость: прямое добавление .css к маршруту не сработает.

Но! PHP унаследовал от языка C обработку null-байта. Строка /account/home%00.css обрезается по null-байту, превращаясь в /account/home. И вот тогда маршрут СОВПАДАЕТ — и Laravel возвращает данные пользователя.

**Middleware для отклонения вредоносных URL:**
\`class RejectMaliciousUrls
{
    public function handle($request, $next)
    {
        $path = $request->path();
        // Отклоняем URL с null-байтом
        if (str_contains($path, "\\0")) {
            abort(400, 'Invalid URL');
        }
        // Отклоняем URL с управляющими символами
        if (preg_match('/[\\x00-\\x1f]/', $path)) {
            abort(400, 'Invalid URL');
        }
        return $next($request);
    }
}\`

**Go (net/http): строгий парсинг URL**

Go — уникальный среди популярных бэкенд-фреймворков: он НАМНОГО менее уязвим к WCD, чем другие. И вот почему.

Как Go обрабатывает URL:
- \`http.HandleFunc("/account/home", handler)\` — регистрирует маршрут
- \`http.ServeMux\` ищет точное совпадение или самый длинный подходящий префикс
- Go парсит URL через пакет net/url, который СТРОГО декодирует %-кодировку

**ServeMux и нормализация пути:**
ServeMux имеет встроенную нормализацию:
- /account/home/ → перенаправление на /account/home (без слэша) или наоборот
- Но ServeMux НЕ обрезает спецсимволы вроде %0f

**Go НЕ обрезает %0f по умолчанию:**
URL /account/home%0f.css декодируется в /account/home\\x0f.css. ServeMux ищет точное совпадение: /account/home\\x0f.css ≠ /account/home. Результат: 404 Not Found. Атака НЕ срабатывает!

**Go НЕ обрезает строки по null-байту:**
В отличие от PHP и C, строки в Go НЕ обрезаются по null-байту. Строка "home\\x00.css" в Go — это полная строка из 9 символов. ServeMux не найдёт маршрут /account/home\\x00.css — вернёт 404.

**Но если использовать фреймворк (Gin, Echo):**
Gin и Echo — популярные фреймворки для Go. Они добавляют удобные функции, но могут изменить поведение:
- Gin: \`r.GET("/account/home", handler)\` — точное совпадение. Gin СТРОГО парсит путь, как и ServeMux. Уязвимости нет.
- Echo: аналогично. Строгий роутинг.
- НО: если использовать wildcard-маршруты (\`r.GET("/account/*name", handler)\`), любой URL под /account/ совпадёт — включая /account/home%0f.css. Это потенциальная уязвимость.

**Вывод по Go:** Go — один из САМЫХ безопасных бэкендов для защиты от WCD. Строгий роутинг + отсутствие обрезки по спецсимволам = атаки почти невозможны. Главное — не использовать catch-all маршруты.

**.NET (ASP.NET Core): строгий middleware-pipeline**

ASP.NET Core — фреймворк от Microsoft, работающий на платформе .NET. Он использует middleware pipeline для обработки запросов и строгий роутинг.

**Как ASP.NET Core обрабатывает URL-кодировку:**
ASP.NET Core декодирует URL через \`WebUtility.UrlDecode\`. Этот метод корректно обрабатывает %-кодировку, но НЕ обрезает строки по спецсимволам.

**MapControllerRoute и attribute routing:**
- \`[Route("account/home")]\` — атрибут маршрута на контроллере
- \`[HttpGet("home")]\` — обработчик GET-запроса
- ASP.NET Core ищет точное совпадение маршрута

**Как Kestrel нормализует URL:**
Kestrel — встроенный веб-сервер ASP.NET Core. Он:
1. Декодирует %-кодировку
2. Отклоняет запросы с null-байтом (%00) — возвращает 400 Bad Request
3. Нормализует путь: удаляет двойные слэши, обрабатывает ../
4. Отклоняет запросы с невалидными UTF-8 последовательностями

**ASP.NET Core и точка с запятой:**
В отличие от Spring (Java), ASP.NET Core НЕ обрезает URL по точке с запятой (;). /account/home;.css НЕ совпадает с маршрутом /account/home — вернётся 404. Это хорошо для безопасности.

**Сводная таблица безопасности фреймворков:**

| Фреймворк | %0f | %00 | ;  | .css | Общая безопасность |
|-----------|------|------|-----|------|--------------------|
| Django (path) | Безопасен | Уязвим! | Безопасен | Безопасен | Средняя |
| Django (re_path) | Уязвим! | Уязвим! | Уязвим! | Зависит | Низкая |
| Laravel | Безопасен | Уязвим! | Безопасен | Безопасен | Средняя |
| Go (net/http) | Безопасен | Безопасен | Безопасен | Безопасен | Высокая |
| Go (Gin/Echo) | Безопасен | Безопасен | Безопасен | Зависит | Высокая |
| ASP.NET Core | Безопасен | Безопасен | Безопасен | Безопасен | Высокая |

**Главный вывод:** безопасность фреймворка зависит не только от его встроенных механизмов, но и от того, КАК вы его используете. Catch-all маршруты, wildcard-параметры и отсутствие заголовка Cache-Control: no-store — вот главные причины уязвимости, независимо от фреймворка.`
  }
]
