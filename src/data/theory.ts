import type { TheorySection } from '@/types'

export const theorySections: TheorySection[] = [
  {
    id: 'what-is-cache',
    title: 'Что такое Web Cache',
    icon: 'Database',
    content: `**Web Cache (веб-кэш)** — это промежуточное хранилище, которое сохраняет копии часто запрашиваемых ресурсов (HTML-страницы, изображения, CSS, JavaScript) для ускорения их последующей выдачи. Когда пользователь обращается к веб-сайту, запрос сначала проходит через кэш, и если запрошенный ресурс уже там хранится, кэш немедленно возвращает его, не пересылая запрос на сервер-источник.

Кэширование является одним из фундаментальных механизмов современной веб-инфраструктуры. Без кэширования каждый запрос пользователя доходил бы до сервера-источника, что создавало бы огромную нагрузку на серверы и значительно замедляло бы загрузку страниц, особенно для пользователей, находящихся далеко от серверов.

Веб-кэш может располагаться на разных уровнях инфраструктуры:

- **Браузерный кэш** — хранит ресурсы непосредственно на устройстве пользователя. Когда вы повторно посещаете сайт, браузер может использовать локально сохранённые копии вместо повторной загрузки.
- **CDN (Content Delivery Network)** — распределённая сеть серверов по всему миру, которые кэшируют контент ближе к пользователям. Примеры: Cloudflare, Akamai, AWS CloudFront.
- **Обратный прокси (Reverse Proxy)** — сервер, расположенный перед сервером-источником, который кэширует ответы и обслуживает запросы самостоятельно. Примеры: Nginx, Varnish, HAProxy.
- **Промежуточные прокси** — корпоративные или провайдерские прокси-серверы, которые кэшируют популярный контент для экономии пропускной способности.

Каждый из этих уровней может независимо принимать решения о том, что кэшировать, а что — нет. Именно эта независимость и создаёт возможности для уязвимостей типа Web Cache Deception.`
  },
  {
    id: 'why-caching',
    title: 'Зачем используется кэширование',
    icon: 'Zap',
    content: `Кэширование решает несколько критически важных задач в веб-инфраструктуре:

**1. Ускорение загрузки страниц**
Когда пользователь из Москвы обращается к сайту, серверы которого расположены в США, запросу нужно преодолеть тысячи километров. Кэш на CDN-узле в Европе может вернуть сохранённую копию за миллисекунды вместо сотен миллисекунд при обращении к оригинальному серверу. Это особенно важно для динамичных веб-приложений, где скорость отклика напрямую влияет на пользовательский опыт.

**2. Снижение нагрузки на серверы**
Популярные сайты могут получать миллионы запросов в минуту. Без кэширования серверам пришлось бы обрабатывать каждый запрос индивидуально, генерируя HTML, выполняя запросы к базе данных и собирая ответ. Кэш берёт на себя значительную долю повторяющихся запросов, освобождая серверы для обработки уникальных или динамических запросов.

**3. Экономия пропускной способности**
Передача данных стоит денег, особенно при больших объёмах трафика. Кэширование на уровне CDN или прокси позволяет значительно сократить объём данных, которые нужно передавать между дата-центрами, экономя затраты на пропускную способность.

**4. Повышение доступности**
Если сервер-источник временно недоступен, кэш может продолжать обслуживать сохранённые копии ресурсов, обеспечивая частичную работоспособность сайта. Это повышает отказоустойчивость всей системы.

**5. Защита от пиковых нагрузок**
Во время всплесков трафика (распродажи, вирусные события) кэш поглощает основную нагрузку, предотвращая перегрузку серверов и отказы в обслуживании.

Однако кэширование создаёт и проблемы: кэш может случайно сохранить конфиденциальные данные и выдать их другим пользователям. Именно это и эксплуатирует атака Web Cache Deception.`
  },
  {
    id: 'how-backend-works',
    title: 'Как работает Backend',
    icon: 'Server',
    content: `**Backend (серверная часть)** — это сервер-источник, который обрабатывает запросы, выполняет бизнес-логику и генерирует ответы. В контексте кэширования важно понимать, как именно сервер-источник интерпретирует входящие HTTP-запросы.

Когда запрос достигает сервера-источника, происходит следующая цепочка обработки:

**1. Приём и парсинг запроса**
Сервер получает HTTP-запрос и разбирает его на составные части: метод (GET, POST и т.д.), путь URL, заголовки, тело запроса. Каждый веб-фреймворк имеет собственные правила парсинга URL, что особенно важно для понимания уязвимости WCD.

**2. Маршрутизация (Routing)**
Сервер определяет, какой обработчик (handler) должен обработать данный URL. Например, запрос к "/account/home" направляется к обработчику профиля пользователя, а запрос к "/static/style.css" — к обработчику статических файлов. Ключевой момент: разные серверы могут по-разному интерпретировать один и тот же URL, особенно если он содержит необычные символы.

**3. Аутентификация и авторизация**
Для защищённых ресурсов сервер проверяет, имеет ли пользователь право на доступ. Обычно это делается через cookies, токены или заголовки Authorization. Результат авторизации — конкретные данные пользователя (например, его профиль).

**4. Выполнение бизнес-логики**
Сервер обращается к базе данных, выполняет вычисления, вызывает внешние API — всё, что необходимо для формирования ответа.

**5. Формирование ответа**
Сервер генерирует HTTP-ответ с соответствующими заголовками и телом. Для динамических страниц ответ содержит персонализированные данные. Для статических файлов — содержимое файла.

Критически важное отличие: Backend обычно игнорирует символы после пути, которые он распознаёт, или интерпретирует их по собственным правилам. Например, запрос "/account/home%0f.css" может быть распознан сервером как "/account/home" — путь к профилю пользователя, а не как статический CSS-файл.`
  },
  {
    id: 'what-is-wcd',
    title: 'Что такое Web Cache Deception',
    icon: 'ShieldAlert',
    content: `**Web Cache Deception (WCD)** — это уязвимость, при которой атакующий обманывает кэш-сервер, заставляя его сохранить и выдать другим пользователям конфиденциальные данные жертвы.

Суть атаки строится на разнице в том, как кэш и сервер-источник интерпретируют один и тот же URL. Атакующий создаёт специальный URL, который:

- **Кэш воспринимает как запрос к статическому ресурсу** (например, CSS, JS, изображение), который можно безопасно кэшировать и выдавать всем пользователям.
- **Сервер-источник воспринимает как запрос к динамическому, защищённому ресурсу** (например, профилю пользователя), который содержит персональные данные.

**Сценарий атаки по шагам:**

1. Атакующий анализирует целевой сайт и обнаруживает расхождения в обработке URL между кэшем и сервером.
2. Атакующий создаёт вредоносную ссылку, например: \`https://example.com/account/home%0f.css\`
3. Жертва переходит по этой ссылке (через фишинг, социальную инженерию или внедрение в страницу).
4. Кэш видит расширение ".css" и решает, что это статический файл — кэширует ответ.
5. Сервер-источник игнорирует "%0f.css" и обрабатывает запрос как "/account/home", возвращая профиль жертвы с конфиденциальными данными.
6. Ответ кэшируется как "статический CSS-файл".
7. Атакующий обращается к тому же URL и получает кэшированный ответ с данными жертвы.

**Почему это опасно?**
Уязвимость позволяет получить доступ к чувствительным данным: персональной информации, API-токенам, финансовым данным, сессионным идентификаторам — всему, что сервер возвращает в защищённых страницах. При этом атака не требует технических навыков высокого уровня и может быть автоматизирована.

WCD была впервые описана исследователем Омелом Халилем (Omer Halil) в 2017 году и с тех пор обнаруживалась на множестве крупных платформ, включая Facebook, PayPal и другие.`
  },
  {
    id: 'delimiter-discrepancies',
    title: 'Что такое Delimiter Discrepancies',
    icon: 'Split',
    content: `**Delimiter Discrepancies (расхождения разделителей)** — это ключевая концепция, лежащая в основе атаки Web Cache Deception. Речь идёт о том, что разные компоненты веб-инфраструктуры используют разные правила для определения границ частей URL.

**Что такое разделитель (delimiter)?**
В контексте URL разделитель — это символ или последовательность символов, которые разделяют URL на логические части: путь, параметры, расширение файла и т.д. Например:
- \`/\` разделяет сегменты пути
- \`?\` отделяет путь от query-параметров
- \`#\` отделяет фрагмент
- \`.\` обычно указывает на расширение файла

**Проблема расхождений**
Разные компоненты инфраструктуры по-разному интерпретируют специальные символы в URL:

| Символ | Кэш может интерпретировать как | Backend может интерпретировать как |
|--------|-------------------------------|-----------------------------------|
| \`%0f\` | Часть пути (после него ".css" — расширение) | Разделитель (игнорируется всё после) |
| \`%0a\` | Часть пути | Разделитель строки (обрезает путь) |
| \`%00\` | Часть пути | Null-байт (обрезает путь) |
| \`;\` | Часть пути или параметр | Разделитель параметров пути |
| \`?\` | Начало query-параметров | Начало query-параметров |
| \`..\` | Часть пути | Переход на уровень выше |

**Пример расхождения:**
URL: \`/account/home%0f.css\`

- **Кэш (Cloudflare/Nginx)** видит путь, заканчивающийся на \`.css\`, и решает: "Это статический файл CSS, нужно кэшировать".
- **Backend (Node.js/Python/Java)** видит путь \`/account/home\`, потому что символ \`%0f\` (Shift In — непечатный управляющий символ) воспринимается как разделитель или просто игнорируется.

**Почему возникают расхождения?**
1. **Разные RFC и стандарты** — Существует несколько стандартов URL (RFC 3986, RFC 2396 и др.), и компоненты могут следовать разным версиям.
2. **Разные библиотеки парсинга** — Кэш может использовать одну библиотеку для парсинга URL, а сервер — другую.
3. **Нормализация URL** — Некоторые компоненты нормализуют URL перед обработкой, декодируя или удаляя определённые символы.
4. **Специфичные реализации** — Каждый веб-сервер и фреймворк имеет свои особенности обработки URL.

Именно существование этих расхождений позволяет атакующему создать URL, который будет интерпретирован по-разному разными компонентами инфраструктуры.`
  },
  {
    id: 'url-interpretation',
    title: 'Как компоненты по-разному интерпретируют URL',
    icon: 'GitBranch',
    content: `Каждый компонент веб-инфраструктуры имеет собственную логику обработки URL. Понимание этих различий — ключ к освоению Web Cache Deception.

**1. CDN (например, Cloudflare)**
CDN обычно действует как первый уровень обработки запроса. Его задача — быстро определить, есть ли закэшированная копия. Для этого CDN:
- Анализирует путь URL для определения типа ресурса
- Использует расширение файла как главный критерий: если URL заканчивается на \`.css\`, \`.js\`, \`.png\` — ресурс считается статическим
- Применяет собственные правила кэширования, которые могут не совпадать с правилами сервера-источника
- Может игнорировать или по-своему обрабатывать специальные символы в URL

**2. Обратный прокси (Nginx, Varnish)**
Обратный прокси стоит между CDN и сервером-источником:
- Nginx по умолчанию передаёт URL как есть, но его конфигурация может изменять путь
- Varnish использует VCL (Varnish Configuration Language) для гибкой обработки URL
- Оба могут применять правила перезаписи URL, которые меняют путь перед отправкой на сервер
- Правила определения статических ресурсов могут отличаться от CDN

**3. Сервер-источник (Backend)**
Backend — конечный обработчик запроса:
- Node.js (Express): обычно декодирует URL и обрабатывает путь по правилам маршрутизации
- Python (Django/Flask): может игнорировать символы после определённого разделителя
- Java (Spring): применяет собственные правила маппинга URL
- Ruby (Rails): может интерпретировать точки в пути как спецификаторы формата

**Конкретные примеры расхождений:**

| URL | CDN/Кэш видит | Backend видит |
|-----|--------------|---------------|
| \`/api/user%0f.css\` | Статический CSS файл | \`/api/user\` — данные пользователя |
| \`/profile.js\` | Статический JS файл | \`/profile\` в формате .js |
| \`/account;admin.css\` | Статический CSS файл | \`/account\` с параметром пути |
| \`/dashboard%00.css\` | Статический CSS файл | \`/dashboard\` (null-byte обрезает) |
| \`/settings%0a.css\` | Статический CSS файл | \`/settings\` (перевод строки обрезает) |

Эти расхождения — не баги отдельных компонентов, а следствие того, что каждый компонент разрабатывался независимо, следуя разным стандартам и требованиям. Атакующий использует эти естественные различия для конструирования вредоносных URL.`
  },
  {
    id: 'data-leakage',
    title: 'Как возникает утечка данных',
    icon: 'AlertTriangle',
    content: `Утечка данных через Web Cache Deception происходит в результате цепочки событий, где каждый шаг использует расхождение в поведении компонентов инфраструктуры. Давайте разберём эту цепочку подробно.

**Полный цикл утечки данных:**

**Шаг 1: Подготовка атаки**
Атакующий изучает целевой сайт, определяя:
- Какие страницы возвращают конфиденциальные данные (профиль, настройки, API-ключи)
- Какой кэш используется (по заголовкам ответов: CF-Ray, X-Cache, Via и т.д.)
- Какие разделители вызывают расхождения между кэшем и сервером

**Шаг 2: Конструирование вредоносного URL**
Атакующий создаёт URL, который выглядит как статический ресурс для кэша, но как динамический — для сервера. Например:
\`https://site.com/account/home%0f.css\`

**Шаг 3: Привлечение жертвы**
Жертва должна перейти по вредоносной ссылке. Способы:
- Фишинговое письмо с ссылкой
- Внедрение ссылки в комментарий или форум на целевом сайте
- Внедрение через XSS (если есть)
- Социальная инженерия

**Шаг 4: Запрос жертвы через кэш**
Когда жертва переходит по ссылке:
- Запрос поступает на кэш
- Кэш не находит закэшированной копии (первый запрос к этому URL)
- Кэш пересылает запрос на сервер-источник

**Шаг 5: Обработка сервером**
Сервер-источник:
- Интерпретирует URL как запрос к защищённой странице
- Проверяет аутентификацию жертвы (по cookie)
- Возвращает персональные данные жертвы в теле ответа

**Шаг 6: Кэширование ответа**
Кэш получает ответ от сервера и:
- Видит, что URL выглядит как статический ресурс (расширение .css)
- Решает кэшировать ответ, возможно, с длительным TTL (Time To Live)
- Сохраняет ответ с конфиденциальными данными жертвы

**Шаг 7: Извлечение данных**
Атакующий отправляет запрос к тому же URL:
- Кэш находит закэшированную копию
- Возвращает ответ с данными жертвы атакующему
- Никакой аутентификации не требуется — кэш отдаёт "статический файл" всем

**Факторы, усиливающие утечку:**
- Длительный TTL кэша — данные доступны долго
- Отсутствие заголовков Cache-Control — кэш кэширует "по умолчанию"
- Несколько уровней кэша — данные распространяются шире
- Высокий трафик — больше шансов, что жертва перейдёт по ссылке

**Защита от утечки:**
- Устанавливать заголовки \`Cache-Control: no-store\` для динамических страниц
- Нормализовать URL на уровне кэша
- Использовать Vary-заголовки для разделения кэша по пользователям
- Проверять расширения файлов на сервере и отклонять подозрительные запросы`
  }
]
